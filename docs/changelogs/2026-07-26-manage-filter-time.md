# 管理页：类型过滤 + 时间列 + 时间排序

- 工具栏新增"类型"下拉过滤：全部 / rule / pitfall / note / reference / 仅草稿
  （客户端过滤，即时生效）。
- 条目列表新增"时间"列（置于标题前，文件 mtime，YYYY-MM-DD HH:mm），
  默认按时间倒序排列（原文件名排序废弃）；搜索命中时命中项仍置顶、组内
  保持时间倒序。
- `/api/entries` 响应条目新增 `mtime` 字段（文件修改时间 unix 秒）。
