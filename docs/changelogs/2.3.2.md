# 2.3.2

## 新功能
- hooks 超时可配置：三条 hook 统一取全局配置 `[hooks] timeout_sec`（默认 10，原 10/5/5）；GUI 引导页新增独立"hook 超时"卡（全局统一、非按 agent 分别配置，保存后自动重写所有已检测 agent 的 hooks），`POST /api/setup/hooks` 接受 `{"timeout_sec": N}` 先落盘再安装；`/api/status` 返回 `hooksTimeout`
- 经验沉淀分流"新需求"到 wiki：`openknowledge-propose` 技能新增"先分类"指引（经验型→propose 草稿 / 结构型→openknowledge-wiki 补 wiki / 兼有→都记）；`ok search` 在检索主题无 wiki 条目覆盖时输出兜底提示行（`index.HasWikiMatch`：FTS + draft=0 + tags 含 wiki，fail-open）

## 修复
- `HandlePostTool` 三个静默返回分支（parse 失败、项目定位失败、路径不在项目内）补 ok.log 日志——2026-08-04 曾出现整会话 14 次编辑 touched 全丢且无任何痕迹，疑似 5s 超时在高负载下被 kimi 静默杀死；无日志即说明 kimi 未派发
