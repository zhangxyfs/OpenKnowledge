# 2.10.1

## 修复
- 导出/导入纳入 wiki 状态：state/wiki.json（基准分支 + 按分支游标 + 合并谱系）
  随 zip 包备份，导入时原样还原。此前导出范围只有条目与 config——删除项目
  知识库的「导出 zip 备份」因此不含谱系，重导入后 GUI 谱系行归零、wiki
  status/diff 游标丢失导致全量重扫（v2.8 引入、v2.9 删除备份场景放大）

## 说明
- 旧备份包无 wiki.json 时跳过还原，向后兼容；条目索引仍在导入时自动重建
