# GUI 布局与导航清理

- 主容器取消 `max-width: 1100px` 居中限制：改为全宽 + 32px 内边距，
  管理页表格与引导页卡片在最大化窗口下占满空间。
- 移除导航条上的 "OpenKnowledge" 品牌文字（`.brand` 及其样式）。
- 移除"退出服务"按钮与对应 JS/CSS：关闭页面后心跳停止、服务 30s 自动退出，
  按钮属于冗余；`/api/shutdown` 端点保留供程序化调用。
