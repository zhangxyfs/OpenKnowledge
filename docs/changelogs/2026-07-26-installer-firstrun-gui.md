# 安装完成页首次引导改为启动 GUI

- 问题：安装完成页的"运行首次引导"勾选后看似没运行。原因：ok.exe 为 GUI
  子系统编译（无控制台），而 `ok setup` 的 embedding 交互提问依赖 stdin，
  安装器启动时 stdin 无效 → 空答案跳过、静默退出，用户无感知。
- 修复：完成页不再带 `setup` 参数，改为直接启动 ok.exe（即打开 GUI），
  首次引导全部由引导页的三张一键卡片（hooks/技能/embedding）承担——
  这才是 GUI 子系统下正确的首次引导入口。
