# 三进程分离（ok / okd / OkManager）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把单 exe（`cmd/ok` 内嵌 CLI + hook + daemon + GUI）拆成 ok.exe（CLI/hook 客户端）、okd.exe（常驻 daemon）、OkManager.exe（配置中心薄启动器）三个进程，行为对外不变。

**Architecture:** daemon 与 GUI 本就同端口同服务（`internal/daemon/run.go:72-75` 挂 `gui.NewHandler`），本计划是纯提取 + 拉起目标切换：`internal/daemon` 的 spawn 目标从"自身 exe + `daemon` 参数"改为"同目录 okd.exe"，旧路径保留为回退；`cmd/okd`、`cmd/okmanager` 为两个几十行的新 main。配置中心 Web UI 重写不在本计划内（分离落地后单独出计划，以 `web/prototype-manager-v2.html` 为准）。

**Tech Stack:** Go 1.25（无新增第三方依赖）、Inno Setup 7、nfpm、go-winres。

**Spec:** `docs/2026-08-21-gui-split-design.md`

## Global Constraints

- 不新增任何第三方 Go 依赖（托盘已是 `internal/tray` 手写 Win32 实现，无需 systray 库）
- 提交信息沿用仓库惯例：Conventional Commits 前缀 + 中文描述（如 `feat(gui): …`、`chore(installer): …`）
- 每个 Task 结束 `go build ./... && go test ./...` 必须全绿才允许 commit
- Windows 为主战场，Linux 构建保持可用（`scripts/build-linux.sh`）
- hook 本地 fallback 是有意保留的设计例外（设计文档 §4.1），任何 task 不得删除
- 测试布局：黑盒 e2e 集中 `tests/e2e/`，白盒单测与被测包同目录（Go 测试可见性约束，取舍见 Task 7 说明）

## 对设计文档的两处修订（探索代码后的证据驱动，Task 7 回写设计文档）

1. **CLI 写操作不转发 okd**。设计文档 §4.1 说"所有写操作子命令改为转发 okd"。实际架构里条目/配置就是普通文件，多写者一致性已由 mtime 增量 `db.Sync` 保证（hook 每轮 `internal/hook/core.go:35-41`、GUI 每请求 `internal/gui/api.go:636`、CLI 写后 `internal/cli/cli.go:259-297`），转发只会给每个 CLI 命令增加 daemon 存活依赖与延迟，零一致性收益。"唯一写者"重新定义为：okd 是 sidecar 托管 / 管理 API / 托盘的唯一持有者。
2. **ok.exe 保留隐藏 `daemon` 分支**。设计文档说"拿掉"。保留理由：它是 spawn 的回退目标（旧部署/单二进制开发模式，见 Task 1），且 `daemon stop` 是安装器在用的纯客户端操作；移除零收益、破坏回退。分支加弃用标注。

---

### Task 1: daemon 拉起目标解耦（daemonTarget + FingerprintOf + SpawnDetached 带参数）

**Files:**
- Create: `internal/daemon/target.go`
- Modify: `internal/daemon/client.go:27-76`（SpawnDetached 声明、Ensure、EnsureCurrent）
- Modify: `internal/daemon/spawn_windows.go:11-25`、`internal/daemon/spawn_other.go:10-23`（签名加 args）
- Modify: `internal/daemonx/daemonx.go:72-87`（ExeFingerprint 拆出 FingerprintOf）
- Test: `internal/daemon/target_test.go`（新建）、`internal/daemon/client_test.go:16-24`（stubSpawn 签名）

**Interfaces:**
- Consumes: 现有 `daemonx.Save/Load/Healthy/Shutdown`、`registry.Home()`
- Produces:
  - `daemonTargetFor(self string) (exe string, args []string)` — 纯函数，供测试
  - `daemonTarget() (string, []string)` — 基于 `os.Executable()` 解析
  - `daemonx.FingerprintOf(exe string) (string, error)`；`ExeFingerprint()` 语义不变
  - `SpawnDetached` 新签名 `func(exe string, args []string, logPath string) error`

背景（必须在 commit message 里体现动机）：拆分后 `EnsureCurrent` 用 `daemonx.ExeFingerprint()`（=调用方 ok.exe 的指纹）对比 daemon.json 里 okd.exe 写入的指纹，必然不一致 → 无限重启 daemon。指纹必须对**拉起目标**取。

- [ ] **Step 1: 写失败测试 `internal/daemon/target_test.go`**

```go
package daemon

import (
	"os"
	"path/filepath"
	"testing"
)

func TestDaemonTargetFor(t *testing.T) {
	dir := t.TempDir()
	self := filepath.Join(dir, "ok.exe")
	if err := os.WriteFile(self, []byte("x"), 0o755); err != nil {
		t.Fatal(err)
	}
	// 无同目录 okd：回退自身 + "daemon"（单二进制/旧部署）
	exe, args := daemonTargetFor(self)
	if exe != self || len(args) != 1 || args[0] != "daemon" {
		t.Fatalf("fallback: exe=%q args=%v", exe, args)
	}
	// 有同目录 okd：用 okd、无参数（三 exe 部署）
	okd := filepath.Join(dir, daemonExeName())
	if err := os.WriteFile(okd, []byte("x"), 0o755); err != nil {
		t.Fatal(err)
	}
	exe, args = daemonTargetFor(self)
	if exe != okd || len(args) != 0 {
		t.Fatalf("sibling okd: exe=%q args=%v", exe, args)
	}
}
```

- [ ] **Step 2: 验证变红**

Run: `go test ./internal/daemon/ -run TestDaemonTargetFor -v`
Expected: FAIL — `undefined: daemonTargetFor`（编译错误即红；行为变化类测试须先见红）

- [ ] **Step 3: 实现 `internal/daemon/target.go`**

```go
package daemon

import (
	"os"
	"path/filepath"
	"runtime"
)

// daemonExeName 三 exe 部署中 daemon 的可执行文件名。
func daemonExeName() string {
	if runtime.GOOS == "windows" {
		return "okd.exe"
	}
	return "okd"
}

// daemonTargetFor 由 self（当前 exe 的绝对路径）推导拉起 daemon 的目标与参数：
// 同目录存在 okd（三 exe 部署）→ 它、无参数；否则 → 自身 + "daemon"（单二进制开发/旧部署回退）。
func daemonTargetFor(self string) (string, []string) {
	sibling := filepath.Join(filepath.Dir(self), daemonExeName())
	if _, err := os.Stat(sibling); err == nil {
		return sibling, nil
	}
	return self, []string{"daemon"}
}

// daemonTarget 当前进程的拉起目标（符号链接解析后）。os.Executable 失败返回空串。
func daemonTarget() (string, []string) {
	self, err := os.Executable()
	if err != nil {
		return "", nil
	}
	if resolved, err := filepath.EvalSymlinks(self); err == nil {
		self = resolved
	}
	return daemonTargetFor(self)
}
```

- [ ] **Step 4: 验证变绿**

Run: `go test ./internal/daemon/ -run TestDaemonTargetFor -v`
Expected: PASS

- [ ] **Step 5: `daemonx.ExeFingerprint` 拆出 `FingerprintOf`（`internal/daemonx/daemonx.go:72-87`）**

替换整个 `ExeFingerprint` 函数为：

```go
// FingerprintOf 返回指定可执行文件的指纹："路径|size|mtimeUnixNano"。
// exe 升级（重装/重新构建）后指纹必然变化，供版本不一致自动切换。
func FingerprintOf(exe string) (string, error) {
	if resolved, err := filepath.EvalSymlinks(exe); err == nil {
		exe = resolved
	}
	fi, err := os.Stat(exe)
	if err != nil {
		return "", err
	}
	return fmt.Sprintf("%s|%d|%d", filepath.ToSlash(exe), fi.Size(), fi.ModTime().UnixNano()), nil
}

// ExeFingerprint 返回当前可执行文件的指纹（= FingerprintOf(自身)）。
// daemon 进程内自报指纹用它；客户端比对请用 FingerprintOf(拉起目标)。
func ExeFingerprint() (string, error) {
	exe, err := os.Executable()
	if err != nil {
		return "", err
	}
	return FingerprintOf(exe)
}
```

- [ ] **Step 6: SpawnDetached 加 args 参数**

`internal/daemon/spawn_windows.go`：注释行与函数改为

```go
// spawnDetached 以 DETACHED_PROCESS 后台启动 daemon 进程，stdio 写入日志文件。
func spawnDetached(exe string, args []string, logPath string) error {
	f, err := os.OpenFile(logPath, os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0o644)
	if err != nil {
		return err
	}
	defer f.Close()
	cmd := exec.Command(exe, args...)
	cmd.Stdout, cmd.Stderr = f, f
	// DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP：不继承父控制台，父退出后继续存活
	cmd.SysProcAttr = &syscall.SysProcAttr{CreationFlags: 0x00000008 | 0x00000200}
	if err := cmd.Start(); err != nil {
		return err
	}
	return cmd.Process.Release()
}
```

`internal/daemon/spawn_other.go`：同样把 `exec.Command(exe, "daemon")` 改为 `exec.Command(exe, args...)`，签名同步改为 `(exe string, args []string, logPath string)`。

`internal/daemon/client.go:27-28` 声明改为：

```go
// SpawnDetached 后台拉起 daemon（目标与参数由 daemonTarget 决定）；测试可替换。
var SpawnDetached = spawnDetached
```

- [ ] **Step 7: Ensure / EnsureCurrent 改用 daemonTarget（client.go:32-76）**

`Ensure` 中删除 `exe, err := os.Executable()` 到 `SpawnDetached(...)` 一段（client.go:47-54），替换为：

```go
	exe, args := daemonTarget()
	if exe == "" {
		return
	}
	_ = SpawnDetached(exe, args, filepath.Join(registry.Home(), "daemon.log"))
```

`EnsureCurrent` 的指纹比较行（client.go:69）替换为：

```go
	target, _ := daemonTarget()
	if cur, err := daemonx.FingerprintOf(target); err != nil || cur != info.Fingerprint {
```

（`daemonTarget` 返回空串时 `FingerprintOf("")` 必报错，走同一重启分支，语义不变。）

- [ ] **Step 8: 更新 stubSpawn 签名（client_test.go:17-24）**

```go
// stubSpawn 替换 SpawnDetached，返回调用次数。
func stubSpawn(t *testing.T) *int {
	t.Helper()
	calls := new(int)
	old := SpawnDetached
	SpawnDetached = func(exe string, args []string, logPath string) error { *calls++; return nil }
	t.Cleanup(func() { SpawnDetached = old })
	return calls
}
```

- [ ] **Step 9: 全量验证**

Run: `go build ./... && go test ./internal/daemon/ ./internal/daemonx/ -v`
Expected: 全部 PASS（既有测试在单二进制回退路径下语义不变：测试进程同目录无 okd → `daemonTargetFor` 回退自身，指纹与 `ExeFingerprint()` 一致）

- [ ] **Step 10: Commit**

```bash
git add internal/daemon/target.go internal/daemon/target_test.go internal/daemon/client.go internal/daemon/spawn_windows.go internal/daemon/spawn_other.go internal/daemon/client_test.go internal/daemonx/daemonx.go
git commit -m "refactor(daemon): spawn 目标解耦为同目录 okd 优先、自身+daemon 回退，指纹改对拉起目标取"
```

---

### Task 2: cmd/okd 独立入口

**Files:**
- Create: `cmd/okd/main.go`

**Interfaces:**
- Consumes: `daemon.Run(webDir string, stdout, stderr io.Writer) int`（`internal/daemon/run.go:31`）、`daemon.Stop(stdout, stderr io.Writer) int`（run.go:136）、`logx.New`（用法见 `cmd/ok/main.go:43`）
- Produces: `okd[.exe]`——无参数常驻服务；`okd stop` 经 API 停服。安装器（Task 6）与 spawn 目标（Task 1）依赖其二进制名

- [ ] **Step 1: 写 `cmd/okd/main.go`**

```go
package main

import (
	"fmt"
	"os"
	"path/filepath"

	"openknowledge/internal/daemon"
	"openknowledge/internal/logx"
)

func main() { os.Exit(run(os.Args)) }

// okd：OpenKnowledge 常驻 daemon——管理 API + 托盘 + sidecar 托管 + Web 静态页分发，
// 见 docs/2026-08-21-gui-split-design.md。无参数 → 常驻服务；stop → 经 API 停服。
// 后台拉起时 stdio 即 daemon.log：按行加时间戳，排查"何时发生"不再靠猜。
func run(argv []string) int {
	if len(argv) > 1 && argv[1] == "stop" {
		return daemon.Stop(os.Stdout, os.Stderr)
	}
	webDir, _ := findWebDir() // 找不到 web 目录也能跑（仅无 GUI 静态页）
	return daemon.Run(webDir, logx.New(os.Stdout), logx.New(os.Stderr))
}

// findWebDir 依次尝试 <exe目录>/web 与 <当前目录>/web。（与 cmd/ok/main.go 同源：
// 两个 main 包无法共享未导出函数，保留一份拷贝，改动需同步）
func findWebDir() (string, error) {
	if exe, err := os.Executable(); err == nil {
		if resolved, err := filepath.EvalSymlinks(exe); err == nil {
			exe = resolved
		}
		if dir := filepath.Join(filepath.Dir(exe), "web"); isDir(dir) {
			return dir, nil
		}
	}
	if cwd, err := os.Getwd(); err == nil {
		if dir := filepath.Join(cwd, "web"); isDir(dir) {
			return dir, nil
		}
	}
	return "", fmt.Errorf("未找到 web 资源目录（<exe目录>/web 或 <当前目录>/web）")
}

func isDir(p string) bool {
	info, err := os.Stat(p)
	return err == nil && info.IsDir()
}
```

- [ ] **Step 2: 构建 + 冒烟**

Run: `go build -o dist/okd.exe ./cmd/okd && OK_HOME="$TEMP/okd-smoke" ./dist/okd.exe & sleep 2; cat "$TEMP/okd-smoke/daemon.json"; ./dist/okd.exe stop; echo "stop exit=$?"`
Expected: `daemon.json` 含非空 `port`/`token`；`okd.exe stop` 输出停服结果且 `stop exit=0`（冒烟后 `rm -rf "$TEMP/okd-smoke"`）

- [ ] **Step 3: 全量验证**

Run: `go build ./... && go test ./...`
Expected: 全绿（cmd/okd 无测试，行为由 Task 4 的 e2e 覆盖）

- [ ] **Step 4: Commit**

```bash
git add cmd/okd/main.go
git commit -m "feat(daemon): 新增 cmd/okd 独立 daemon 入口（Run/Stop 平移自 ok daemon 子命令）"
```

---

### Task 3: cmd/okmanager 薄启动器

**Files:**
- Create: `cmd/okmanager/main.go`

**Interfaces:**
- Consumes: `daemon.OpenGUI(stdout, stderr io.Writer) int`（`internal/daemon/run.go:149-163`：EnsureCurrent 成功即 `OpenBrowserFunc(info.URL()+"/?token="+info.Token)`，否则轮询 daemon.json 最长 3s——已实现"探测→拉起→开窗"全链路）
- Produces: `OkManager.exe`——配置中心入口，安装包快捷方式（Task 6）指向它

薄启动器四步（探测/拉起/开窗/退出）全部由 `OpenGUI` + `gui.OpenBrowser`（`internal/gui/browser_windows.go:17-33`，--app= 模式、回环 URL 守卫）承载，本 main 不含任何业务逻辑。

- [ ] **Step 1: 写 `cmd/okmanager/main.go`**

```go
package main

import (
	"os"

	"openknowledge/internal/daemon"
)

// OkManager：OpenKnowledge 配置中心入口，薄启动器——确保 okd 在线后以 --app= 模式
// 打开配置中心窗口并退出（不驻留；驻留是 okd 的事）。不含任何业务逻辑，
// 全部功能走 okd 的 HTTP API。见 docs/2026-08-21-gui-split-design.md §4.3。
func main() { os.Exit(daemon.OpenGUI(os.Stdout, os.Stderr)) }
```

- [ ] **Step 2: 构建 + 静态检查**

Run: `go build -o dist/OkManager.exe ./cmd/okmanager && go vet ./cmd/okmanager/`
Expected: 构建成功、vet 无输出。（开窗效果为人工验证项：okd 在跑时双击 OkManager.exe 应以应用窗口打开配置中心）

- [ ] **Step 3: Commit**

```bash
git add cmd/okmanager/main.go
git commit -m "feat(gui): 新增 cmd/okmanager 配置中心薄启动器（复用 daemon.OpenGUI）"
```

---

### Task 4: ok.exe 标注与集成测试双 exe 化

**Files:**
- Modify: `cmd/ok/main.go:37-43`（daemon 分支弃用标注）、`cmd/ok/main.go:80-115`（hook fallback 例外注释）
- Modify: `cmd/ok/integration_test.go:15-34`（TestMain 构建双 exe）
- Modify: `cmd/ok/daemon_test.go:54`（前台拉起改用 okdPath）

**Interfaces:**
- Consumes: Task 1 的 `daemonTarget`（spawn 优先同目录 okd）、Task 2 的 `cmd/okd`
- Produces: 包级变量 `okdPath string`（与既有 `binPath` 并列），供本包 e2e 测试使用

- [ ] **Step 1: main.go 加标注（不改行为）**

`daemon` 分支（main.go:37-43）上方加注释，分支体不动：

```go
	case "daemon": // 已弃用的兼容入口：生产部署由 okd.exe 承载（cmd/okd），此处仅为
		// spawn 回退（无 okd.exe 的旧部署/单二进制开发）与 `daemon stop` 客户端操作保留。
		// 见 docs/2026-08-21-gui-split-design.md §4.1。
```

`runHook` 头注释（main.go:80-81）扩写为例外条款：

```go
// runHook hook 路径全面 fail-open：panic 也只放行。
// 优先转发给常驻 daemon；daemon 不在则本地直接处理并后台拉起。
// 本地兜底是"单写者"架构中有意保留的例外：daemon 崩溃时 agent 会话不得被卡死，
// daemon 恢复后由 mtime 增量 sync 收编兜底写入。删除此路径需先改设计文档 §4.1。
```

- [ ] **Step 2: TestMain 构建双 exe（integration_test.go:15-34）**

替换 `var binPath string` 与整个 `TestMain`：

```go
var binPath string
var okdPath string

func TestMain(m *testing.M) {
	dir, err := os.MkdirTemp("", "ok-bin")
	if err != nil {
		panic(err)
	}
	name, okdName := "ok", "okd"
	if runtime.GOOS == "windows" {
		name, okdName = "ok.exe", "okd.exe"
	}
	binPath = filepath.Join(dir, name)
	okdPath = filepath.Join(dir, okdName)
	build := exec.Command("go", "build", "-o", binPath, ".")
	if out, err := build.CombinedOutput(); err != nil {
		panic(fmt.Sprintf("build ok failed: %v\n%s", err, out))
	}
	// 同目录提供 okd：spawn 走三 exe 拆分路径（daemonTarget 命中同目录 okd）
	build = exec.Command("go", "build", "-o", okdPath, "../okd")
	if out, err := build.CombinedOutput(); err != nil {
		panic(fmt.Sprintf("build okd failed: %v\n%s", err, out))
	}
	code := m.Run()
	_ = os.RemoveAll(dir)
	os.Exit(code)
}
```

- [ ] **Step 3: daemon_test.go 前台拉起改 okdPath（:54）**

```go
	// 前台拉起 daemon（子进程，测试结束杀掉）
	daemonCmd := exec.Command(okdPath)
```

- [ ] **Step 4: 跑拆分路径 e2e**

Run: `go test ./cmd/ok/ -run TestDaemonEndToEnd -v`
Expected: PASS——`hook prompt` 经 okd 注入（:75）；`ok daemon stop` 后本地兜底仍注入且**后台重新拉起的是 okd.exe**（:92-102 的新 PID 健康断言自动覆盖 spawn 拆分路径与指纹一致性：若 EnsureCurrent 误用 ok.exe 指纹，此测试会因无限重启/不健康而失败）

- [ ] **Step 5: 全量验证**

Run: `go build ./... && go test ./...`
Expected: 全绿

- [ ] **Step 6: Commit**

```bash
git add cmd/ok/main.go cmd/ok/integration_test.go cmd/ok/daemon_test.go
git commit -m "test(ok): e2e 双 exe 化——okd 同目录构建，前台拉起与 spawn 均走拆分路径；daemon 分支/hook 兜底补设计标注"
```

---

### Task 5: 构建脚本三 exe 化

**Files:**
- Create: `cmd/okd/winres.json`、`cmd/okmanager/winres.json`
- Modify: `scripts/build.py:70-94`（winres 循环 + 三 exe 构建）
- Modify: `scripts/build-dist.sh:6,10`
- Modify: `scripts/build-linux.sh:13-18,42,46-47`

**Interfaces:**
- Consumes: Task 2/3 的新 cmd 包；go-winres（沿用 cmd/ok 既有约定，生成物 `.syso` 随仓库提交）
- Produces: `dist/ok.exe`、`dist/okd.exe`、`dist/OkManager.exe`（Task 6 安装器引用的文件名）；`dist/linux-amd64/okd`

- [ ] **Step 1: 写 `cmd/okd/winres.json`**（与 cmd/ok/winres.json 同构，图标共用）

```json
{
  "RT_GROUP_ICON": {
    "APP": {
      "0000": [
        "../../installer/assets/logo.png"
      ]
    }
  },
  "RT_VERSION": {
    "#1": {
      "0000": {
        "fixed": {
          "file_version": "2.21.0.0",
          "product_version": "2.21.0.0"
        },
        "info": {
          "0409": {
            "FileDescription": "OpenKnowledge daemon (okd)",
            "ProductName": "OpenKnowledge",
            "CompanyName": "OpenKnowledge",
            "LegalCopyright": "OpenKnowledge",
            "OriginalFilename": "okd.exe"
          }
        }
      }
    }
  }
}
```

- [ ] **Step 2: 写 `cmd/okmanager/winres.json`**（同上，仅两处不同）

`"FileDescription": "OkManager - OpenKnowledge 配置中心"`、`"OriginalFilename": "OkManager.exe"`，其余与 Step 1 完全相同。

- [ ] **Step 3: build.py winres 步骤改循环（:70-77）**

```python
    # 1. exe 图标与版本信息（go-winres，缺失时跳过不报错）
    if not args.skip_winres:
        winres = shutil.which("go-winres") or str(Path(os.environ.get("GOPATH", Path.home() / "go")) / "bin" / "go-winres.exe")
        if Path(winres).exists() if not shutil.which("go-winres") else True:
            for pkg in ("ok", "okd", "okmanager"):
                run([winres, "make", "--in", "winres.json"], cwd=ROOT / "cmd" / pkg)
        else:
            print("go-winres 未安装，跳过 exe 图标嵌入")
            print("  安装: go install github.com/tc-hib/go-winres@latest")
```

- [ ] **Step 4: build.py 构建步骤加两个目标（:82 后）**

```python
    run(["go", "build", "-ldflags", ldflags, "-o", "dist/ok.exe", "./cmd/ok"])
    run(["go", "build", "-ldflags", ldflags, "-o", "dist/okd.exe", "./cmd/okd"])
    run(["go", "build", "-ldflags", ldflags, "-o", "dist/OkManager.exe", "./cmd/okmanager"])
```

并把 :94 的完成输出改为 `print("dist/ built: ok.exe + okd.exe + OkManager.exe + web/ + changelogs/")`。

- [ ] **Step 5: build-dist.sh 加两个目标**

第 6 行后插入：

```bash
go build -ldflags "-s -w -H windowsgui -X openknowledge/internal/version.Version=$VERSION" -o dist/okd.exe ./cmd/okd
go build -ldflags "-s -w -H windowsgui -X openknowledge/internal/version.Version=$VERSION" -o dist/OkManager.exe ./cmd/okmanager
```

末行改为 `echo "dist/ built: ok.exe + okd.exe + OkManager.exe + web/ + changelogs/"`。

- [ ] **Step 6: build-linux.sh 加 okd（Linux 无托盘，tray_other.go 空转；OkManager 不出 Linux 版）**

:15 的 `-o "$STAGE/ok" ./cmd/ok` 后插入：

```bash
GOOS=linux GOARCH=amd64 go build \
  -ldflags "-s -w -X openknowledge/internal/version.Version=$VERSION" \
  -o "$STAGE/okd" ./cmd/okd
```

:18 的 `chmod 0755 "$STAGE/ok"` 改为 `chmod 0755 "$STAGE/ok" "$STAGE/okd"`。
:42 的拷贝行改为 `cp -r "$STAGE/ok" "$STAGE/okd" "$STAGE/web" "$STAGE/changelogs" "$STAGE/runtime" "dist/$PKG/"`。
:46-47 两行 tar 命令中的 `$PKG/ok` 均改为 `$PKG/ok "$PKG/okd"`（注意保持引号与续行）：

```bash
tar -cf "$TAR" -C dist --exclude="$PKG/ok" --exclude="$PKG/okd" --exclude="$PKG/runtime/llama-server" "$PKG"
tar -rf "$TAR" -C dist --mode=0755 "$PKG/ok" "$PKG/okd" "$PKG/runtime/llama-server"
```

- [ ] **Step 7: 生成 syso + 全量构建验证**

Run: `python scripts/build.py --skip-installer`
Expected: 输出含三 exe 的 build 命令与 `dist/ built: ok.exe + okd.exe + OkManager.exe + web/ + changelogs/`；`ls -la dist/ok.exe dist/okd.exe dist/OkManager.exe` 三个文件均在。go-winres 未安装时改跑 `go build ./... && go test ./...` 并在 commit 中说明 syso 未再生（`cmd/okd`、`cmd/okmanager` 的 `.syso` 生成物须一并提交，沿用 cmd/ok 惯例）

- [ ] **Step 8: Commit**

```bash
git add cmd/okd/winres.json cmd/okmanager/winres.json cmd/okd/*.syso cmd/okmanager/*.syso scripts/build.py scripts/build-dist.sh scripts/build-linux.sh
git commit -m "build: dist 三 exe 化（ok/okd/OkManager），winres 三包循环，Linux 包加 okd"
```

---

### Task 6: 安装器三 exe 化

**Files:**
- Modify: `installer/openknowledge.iss:35-51`（Files/Icons/Registry/Run）、`:129,142`（停 daemon 的 Exec）
- Modify: `installer/nfpm.yaml:12-29`

**Interfaces:**
- Consumes: Task 5 的产物文件名（`dist/okd.exe`、`dist/OkManager.exe`、`dist/linux-amd64/okd`）；`okd stop`（Task 2）
- Produces: 安装后 `{app}\okd.exe`、`{app}\OkManager.exe`；开始菜单/桌面快捷方式指向 OkManager.exe；开机自启 Run 键指向 okd.exe（无参数）

- [ ] **Step 1: iss [Files] 加两条（:36 后）**

```iss
Source: "..\dist\okd.exe"; DestDir: "{app}"; Flags: ignoreversion
Source: "..\dist\OkManager.exe"; DestDir: "{app}"; Flags: ignoreversion
```

- [ ] **Step 2: iss [Icons] 快捷方式指向 OkManager（:43,45）**

```iss
Name: "{group}\OpenKnowledge 知识库"; Filename: "{app}\OkManager.exe"; IconFilename: "{app}\logo.ico"; Comment: "打开 OpenKnowledge 配置中心"
Name: "{group}\卸载 OpenKnowledge"; Filename: "{uninstallexe}"
Name: "{autodesktop}\OpenKnowledge 知识库"; Filename: "{app}\OkManager.exe"; IconFilename: "{app}\logo.ico"; Tasks: desktopicon
```

- [ ] **Step 3: iss 开机自启 Run 键改 okd（:48）**

```iss
Root: HKCU; Subkey: "Software\Microsoft\Windows\CurrentVersion\Run"; ValueType: string; ValueName: "OpenKnowledge"; ValueData: """{app}\okd.exe"""; Flags: uninsdeletevalue
```

（设计文档 §8 待定问题 1 的落地选择：维持"安装即注册自启"现状，仅换目标；是否改为懒拉起留待后续版本。）

- [ ] **Step 4: iss [Run] postinstall 改 OkManager（:51）**

```iss
Filename: "{app}\OkManager.exe"; Description: "打开 OpenKnowledge 配置中心（引导页可一键完成 hooks / 技能 / embedding 配置）"; Flags: postinstall skipifsilent unchecked
```

- [ ] **Step 5: iss 两处停 daemon 的 Exec 改 okd（:129,142）**

```pascal
    { 文件拷贝前停常驻 daemon（hooks 会按需自动拉起它锁住 okd.exe；不存在则 Exec 失败无害） }
    Exec(ExpandConstant('{app}\okd.exe'), 'stop', '', SW_HIDE, ewWaitUntilTerminated, ResultCode);
```

卸载处（:142）同样改为 `Exec(ExpandConstant('{app}\okd.exe'), 'stop', '', SW_HIDE, ewWaitUntilTerminated, ResultCode);`，注释中"ok.exe 立即返回 0"改为"okd.exe 立即返回 0"。

- [ ] **Step 6: nfpm.yaml 加 okd（:14 后；无 /usr/bin 符号链接——内部进程，不占用户 PATH）**

```yaml
  - src: dist/linux-amd64/okd
    dst: /usr/lib/openknowledge/okd
    file_info:
      mode: 0755
```

- [ ] **Step 7: 安装包构建验证**

Run: `python scripts/build.py`
Expected: Inno 编译成功，`installer/output/` 出新安装包；人工项：安装后开始菜单快捷方式打开配置中心、`tasklist | findstr okd` 可见自启驻留。无 ISCC 环境时执行 `bash scripts/build-dist.sh` 并人工核对本 task 的 iss diff

- [ ] **Step 8: Commit**

```bash
git add installer/openknowledge.iss installer/nfpm.yaml
git commit -m "chore(installer): 安装包三 exe 化——快捷方式/自启指向 OkManager/okd，停服调用改 okd stop"
```

---

### Task 7: e2e 测试提取到 tests/e2e（测试与正式代码分离）

**Files:**
- Move: `cmd/ok/integration_test.go` → `tests/e2e/integration_test.go`
- Move: `cmd/ok/daemon_test.go` → `tests/e2e/daemon_test.go`
- Move: `cmd/ok/wiki_test.go` → `tests/e2e/wiki_test.go`

**Interfaces:**
- Consumes: Task 4 落地后的测试内容（TestMain 已构建双 exe）；`daemonx.Load/Info.Healthy`（internal 包同模块内可 import）
- Produces: `tests/e2e` 包（`package e2e`），对外契约不变：`go test ./tests/e2e/ -v`

**为什么只提取这三个文件（取舍说明，commit message 同此）**：Go 的 `*_test.go` 只有与被测包同目录才能访问未导出标识符；放进 `tests/` 的测试只能以普通包 import 被测包、只能访问**导出**面。全仓 83 个 `_test.go` 中 80 个是白盒单测（依赖未导出钩子：`forwardTimeout`、`trayEnabled`、`daemonTargetFor`、内部 helper 等），全部提取意味着要么把大批内部钩子导出（污染 API、违背封装），要么放弃白盒覆盖。Go 的标准布局是"单测同目录、e2e 集中"（标准库与 Kubernetes 均如此）。`cmd/ok` 的 3 个测试文件是纯黑盒（只经 `runOK` 跑子进程 + `binPath/okdPath`，不碰未导出标识符），是唯一可无损提取的部分。

- [ ] **Step 1: 移动文件并改包名**

```bash
mkdir -p tests/e2e
git mv cmd/ok/integration_test.go tests/e2e/integration_test.go
git mv cmd/ok/daemon_test.go tests/e2e/daemon_test.go
git mv cmd/ok/wiki_test.go tests/e2e/wiki_test.go
```

三个文件的包声明行（各文件第 1 行）由 `package main` 改为 `package e2e`。

- [ ] **Step 2: TestMain 构建目标改 import path（tests/e2e/integration_test.go）**

Task 4 Step 2 的两条 build 命令中，`"."` 与 `"../okd"` 分别改为按 import path 构建（tests/e2e 目录下相对路径已失效；模块内 import path 构建与 cwd 无关）：

```go
	build := exec.Command("go", "build", "-o", binPath, "openknowledge/cmd/ok")
	if out, err := build.CombinedOutput(); err != nil {
		panic(fmt.Sprintf("build ok failed: %v\n%s", err, out))
	}
	// 同目录提供 okd：spawn 走三 exe 拆分路径（daemonTarget 命中同目录 okd）
	build = exec.Command("go", "build", "-o", okdPath, "openknowledge/cmd/okd")
	if out, err := build.CombinedOutput(); err != nil {
		panic(fmt.Sprintf("build okd failed: %v\n%s", err, out))
	}
```

其余内容（`runOK`、`pinDedupTurnsOff`、`gitCommit`、各 Test 函数）逐字保留，不改测试逻辑。

- [ ] **Step 3: 跑提取后的 e2e**

Run: `go test ./tests/e2e/ -v`
Expected: 全部 PASS（含 TestDaemonEndToEnd 的"ok daemon stop 后本地兜底 + 自动重拉 okd"断言）

- [ ] **Step 4: 全量验证**

Run: `go build ./... && go test ./...`
Expected: 全绿；`go test ./cmd/ok/` 输出 `?  openknowledge/cmd/ok  [no test files]`

- [ ] **Step 5: Commit**

```bash
git add tests/e2e/ cmd/ok/
git commit -m "test: e2e 提取至 tests/e2e（黑盒子进程测试与正式代码分离）；白盒单测保留同目录——Go 测试可见性约束下 tests/ 内只能访问导出面"
```

---

### Task 8: 契约文档与设计文档修订

**Files:**
- Create: `docs/2026-08-21-okd-contract.md`
- Modify: `docs/2026-08-21-gui-split-design.md`（§2 矩阵、§4.1、§8 待定问题 1）

**Interfaces:**
- Consumes: 本计划"对设计文档的两处修订"节；Task 1-6 落地的事实
- Produces: 跨语言消费方（未来 C++ 版 OkManager、第三方客户端）的唯一契约依据

- [ ] **Step 1: 写 `docs/2026-08-21-okd-contract.md`**

```markdown
# okd 进程契约（跨语言共享，改动需同步全部消费方）

日期：2026-08-21 · 消费方：ok.exe（Go）、OkManager.exe（Go，未来可能 C++）

## 实例凭证

- 路径：`~/.openknowledge/daemon.json`（`OK_HOME` 环境变量可覆盖根目录）
- 内容：`{"pid":int,"port":int,"token":"hex16","fingerprint":"路径|size|mtimeUnixNano","started_at":"RFC3339"}`
- 写入：okd 启动时原子写（tmp+rename），权限 0600；退出时仅当 PID 仍是自己才删除
- 端口：首选 17888；被占且已有健康 okd 则新实例直接退出；否则回退随机端口并写回本文件

## 健康检查

`GET http://127.0.0.1:<port>/api/health`，请求头 `X-Ok-Token: <token>`，200 为健康，401 为 token 不符。

## 拉起约定

- 客户端发现顺序：读 daemon.json → 健康则直接用 → 不健康/不存在则拉起
- 拉起目标：当前 exe 同目录的 `okd.exe`（Linux 为 `okd`），无参数；不存在则回退"自身 exe + `daemon` 参数"（旧部署/单二进制开发）
- 拉起方式：DETACHED 后台进程，stdio 追加写入 `~/.openknowledge/daemon.log`（按行带时间戳）
- 防抖：拉起前写 `daemon.json.spawning` 标记，15s 内不重复拉起
- 版本切换：daemon.json 指纹 ≠ 拉起目标指纹 → 客户端先 `POST /api/shutdown` 再删凭证再拉起

## 停服

`POST /api/shutdown`（带 token，尽力而为），随后客户端删除 daemon.json；或执行 `okd stop` / `ok daemon stop`（同一逻辑的 CLI 封装）。

## hook 转发语义（ok.exe 专属）

- `POST /api/hook/<name>[?format=]`，body 为 hook 原始 payload，响应 `{"stdout","stderr","code"}`
- 超时视作已受理（handled）：请求已被接收且处理不可取消，本地兜底会双执行，宁缺毋双
- 仅连接类失败（拒绝连接/不可达/不健康）才走 ok.exe 本地兜底——兜底是有意保留的例外，见设计文档 §4.1

## 数据一致性模型

条目/配置文件为多写者（CLI、GUI、hook 兜底都可写）；一致性不靠单写者，靠各消费方按需 mtime 增量 `db.Sync`。okd "唯一写者"仅指：sidecar 托管、管理 API、托盘。
```

- [ ] **Step 2: 修订设计文档 §4.1 与 §2**

`docs/2026-08-21-gui-split-design.md` §4.1 第一、二条替换为：

```markdown
- CLI 写操作子命令（add/propose/approve/index/archive/on/off/capture…）**保持直接写本地文件，不转发 okd**——条目/配置是普通文件，多写者一致性已由各消费方按需 mtime 增量 `db.Sync` 保证（`internal/hook/core.go:35-41`、`internal/gui/api.go:636`、`internal/cli/cli.go:259-297`），转发只会增加 daemon 存活依赖与延迟。"唯一写者"仅指 okd 独占 sidecar 托管 / 管理 API / 托盘
- `daemon`、`gui` 子命令保留在 ok.exe：`daemon`（隐藏弃用，spawn 回退目标）与 `daemon stop`（安装器在用的客户端操作）；生产 daemon 入口为 `okd.exe`
```

§2 职责矩阵的"数据读写"行改为：

```markdown
| 数据读写 | 直接写文件（多写者，mtime sync 兜底） | sidecar/API/托盘唯一持有者 | 否（走 HTTP API） |
```

§8 待定问题 1 标记为已定："维持安装即注册自启（Run 键目标改为 okd.exe），懒拉起作为 hook/CLI 路径的既有兜底"。

- [ ] **Step 3: Commit**

```bash
git add docs/2026-08-21-okd-contract.md docs/2026-08-21-gui-split-design.md
git commit -m "docs: okd 进程契约落文档；设计文档按代码事实修订（CLI 不转发、daemon 分支保留、自启维持）"
```

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 §4.1→Task 1/4；§4.2→Task 2/5/6；§4.3→Task 3；§5 降级矩阵→Task 1（spawn 回退）+ Task 4（e2e 断言自动重拉）；§7 步骤 5/6→Task 5/6/8；用户追加的测试分离要求→Task 7（e2e 提取；全量提取因 Go 可见性约束不可行，取舍已记录）。§6 配置中心功能面不在本计划（另立计划）
- **占位符扫描**：无 TBD/TODO；所有代码步含完整代码
- **类型一致性**：`SpawnDetached(exe string, args []string, logPath string)` 在 Task 1 定义、stub 同步；`daemonTargetFor/daemonTarget/daemonExeName` 三处使用一致；`okdPath` 在 Task 4 定义、Task 7 随文件平移；`FingerprintOf` 在 Task 1 定义、EnsureCurrent 使用
