# Wiki 功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 OpenKnowledge 增加项目 wiki 能力：`ok wiki status/mark` 管理 git 游标，INDEX.md 注入 Wiki 目录，prompt hook 每会话一次落后提示，`openknowledge-wiki` 技能随安装分发并驱动 AI 完成扫描与写作。

**Architecture:** 技能驱动（CLI 不含 LLM）。新叶子包 `internal/wiki` 管游标与 git 计数；目录查询进 `internal/index`；hook 提示在 `HandlePrompt` 注入文本末尾追加；技能文件用 `go:embed` 进 setupx 的 skillTemplates。

**Tech Stack:** Go 1.25（module `openknowledge`），stdlib `os/exec` 调 git（仓库首个 git 调用先例），BurntSushi/toml，modernc.org/sqlite。

## Global Constraints

- 平台：Windows + Git Bash；测试不得依赖真实 `~/.openknowledge`、`~/.kimi-code`、`~/.agents`。
- 测试隔离：`OK_HOME`、`KIMI_CODE_HOME`、`OK_SKILLS_HOME` 全部用 `t.Setenv` 指到 `t.TempDir()`。
- hook 链路 fail-open：wiki 相关任何失败不得影响注入主流程、不得产生非零退出码。
- 条目模型不改：不加新 type、不动 frontmatter；wiki 条目 = `type: reference` + tags 含 `wiki`。
- 每任务结束 `go build ./... && go vet ./... && go test ./...` 全绿后提交，commit message 用 Conventional Commits 英文。
- spec：`docs/superpowers/specs/2026-07-28-wiki-design.md`。

---

### Task 1: internal/wiki 包（游标 + git 落后计数）

**Files:**
- Create: `internal/wiki/wiki.go`
- Test: `internal/wiki/wiki_test.go`

**Interfaces:**
- Consumes: 无（叶子包，只依赖 stdlib + 外部 git）。
- Produces（后续任务依赖这些签名）:
  - `type Cursor struct { LastCommit string; GeneratedAt time.Time; EntryCount int }`（JSON 字段 `last_commit`/`generated_at`/`entry_count`）
  - `func CursorPath(stateDir string) string`
  - `func LoadCursor(stateDir string) *Cursor`（不存在/损坏返回 nil）
  - `func SaveCursor(stateDir string, c *Cursor) error`
  - `type Status struct { HasWiki bool; LastCommit string; Behind int; Stale bool; Threshold int }`（JSON 字段 `has_wiki`/`last_commit,omitempty`/`behind`/`stale`/`threshold`）
  - `func CheckStatus(stateDir, srcDir string, threshold int) *Status`（git 不可用/非 git/commit 失踪时 Behind=-1）
  - `func HeadCommit(srcDir string) (string, error)`

- [ ] **Step 1: 写失败测试**

`internal/wiki/wiki_test.go`：

```go
package wiki

import (
	"os"
	"os/exec"
	"path/filepath"
	"testing"
	"time"
)

// gitRepo 建一个含 n 个 commit 的临时 git 仓库，返回目录。
func gitRepo(t *testing.T, n int) string {
	t.Helper()
	dir := t.TempDir()
	env := append(os.Environ(),
		"GIT_AUTHOR_NAME=t", "GIT_AUTHOR_EMAIL=t@t",
		"GIT_COMMITTER_NAME=t", "GIT_COMMITTER_EMAIL=t@t")
	run := func(args ...string) {
		cmd := exec.Command("git", args...)
		cmd.Dir = dir
		cmd.Env = env
		if out, err := cmd.CombinedOutput(); err != nil {
			t.Fatalf("git %v: %v\n%s", args, err, out)
		}
	}
	run("init")
	for i := 0; i < n; i++ {
		if err := os.WriteFile(filepath.Join(dir, "f.txt"), []byte{byte(i)}, 0o644); err != nil {
			t.Fatal(err)
		}
		run("add", ".")
		run("commit", "-m", "c")
	}
	return dir
}

func TestCursorRoundTrip(t *testing.T) {
	dir := t.TempDir()
	if LoadCursor(dir) != nil {
		t.Fatal("expected nil cursor for missing file")
	}
	c := &Cursor{LastCommit: "abc123", GeneratedAt: time.Now().Truncate(time.Second), EntryCount: 7}
	if err := SaveCursor(dir, c); err != nil {
		t.Fatal(err)
	}
	got := LoadCursor(dir)
	if got == nil || got.LastCommit != "abc123" || got.EntryCount != 7 {
		t.Fatalf("round trip mismatch: %+v", got)
	}
	if CursorPath(dir) != filepath.Join(dir, "wiki.json") {
		t.Fatalf("unexpected path %q", CursorPath(dir))
	}
}

func TestLoadCursorCorrupt(t *testing.T) {
	dir := t.TempDir()
	if err := os.WriteFile(CursorPath(dir), []byte("{oops"), 0o644); err != nil {
		t.Fatal(err)
	}
	if LoadCursor(dir) != nil {
		t.Fatal("corrupt cursor should load as nil")
	}
}

func TestCheckStatusNoCursor(t *testing.T) {
	repo := gitRepo(t, 3)
	st := CheckStatus(t.TempDir(), repo, 20)
	if st.HasWiki || st.Behind != 3 || st.Stale {
		t.Fatalf("no cursor: %+v", st)
	}
	st = CheckStatus(t.TempDir(), repo, 3)
	if !st.Stale {
		t.Fatalf("full history >= threshold should be stale: %+v", st)
	}
}

func TestCheckStatusBehind(t *testing.T) {
	repo := gitRepo(t, 2)
	head, err := HeadCommit(repo)
	if err != nil {
		t.Fatal(err)
	}
	stateDir := t.TempDir()
	if err := SaveCursor(stateDir, &Cursor{LastCommit: head, GeneratedAt: time.Now()}); err != nil {
		t.Fatal(err)
	}
	// 再追加 2 个 commit
	env := append(os.Environ(), "GIT_AUTHOR_NAME=t", "GIT_AUTHOR_EMAIL=t@t",
		"GIT_COMMITTER_NAME=t", "GIT_COMMITTER_EMAIL=t@t")
	for i := 0; i < 2; i++ {
		if err := os.WriteFile(filepath.Join(repo, "f.txt"), []byte{byte(10 + i)}, 0o644); err != nil {
			t.Fatal(err)
		}
		for _, args := range [][]string{{"add", "."}, {"commit", "-m", "c"}} {
			cmd := exec.Command("git", args...)
			cmd.Dir = repo
			cmd.Env = env
			if out, err := cmd.CombinedOutput(); err != nil {
				t.Fatalf("git %v: %v\n%s", args, err, out)
			}
		}
	}
	st := CheckStatus(stateDir, repo, 2)
	if !st.HasWiki || st.LastCommit != head || st.Behind != 2 || !st.Stale {
		t.Fatalf("behind: %+v", st)
	}
}

func TestCheckStatusGitUnavailable(t *testing.T) {
	// 非 git 目录：behind = -1，不 stale
	st := CheckStatus(t.TempDir(), t.TempDir(), 20)
	if st.Behind != -1 || st.Stale {
		t.Fatalf("non-git: %+v", st)
	}
	// 游标 commit 已不在历史（伪造 hash）
	stateDir := t.TempDir()
	if err := SaveCursor(stateDir, &Cursor{LastCommit: "deadbeefdeadbeefdeadbeefdeadbeefdeadbeef"}); err != nil {
		t.Fatal(err)
	}
	st = CheckStatus(stateDir, gitRepo(t, 1), 20)
	if !st.HasWiki || st.Behind != -1 || st.Stale {
		t.Fatalf("lost commit: %+v", st)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/wiki/`
Expected: 编译失败（package wiki 不存在）。

- [ ] **Step 3: 实现**

`internal/wiki/wiki.go`：

```go
// Package wiki 管理项目 wiki 的游标（state/wiki.json）与 git 落后计数。
// 叶子包：只依赖 stdlib 与外部 git 命令。
package wiki

import (
	"encoding/json"
	"os"
	"os/exec"
	"path/filepath"
	"strconv"
	"strings"
	"time"
)

// Cursor 记录上次 wiki 生成到的位置。
type Cursor struct {
	LastCommit  string    `json:"last_commit"`
	GeneratedAt time.Time `json:"generated_at"`
	EntryCount  int       `json:"entry_count"` // 纯展示用
}

// CursorPath 返回游标文件路径（固定文件名，不受 state 目录 session-* GC 影响）。
func CursorPath(stateDir string) string { return filepath.Join(stateDir, "wiki.json") }

// LoadCursor 读游标；不存在或损坏返回 nil。
func LoadCursor(stateDir string) *Cursor {
	data, err := os.ReadFile(CursorPath(stateDir))
	if err != nil {
		return nil
	}
	var c Cursor
	if json.Unmarshal(data, &c) != nil {
		return nil
	}
	return &c
}

// SaveCursor 写游标（必要时创建 state 目录）。
func SaveCursor(stateDir string, c *Cursor) error {
	if err := os.MkdirAll(stateDir, 0o755); err != nil {
		return err
	}
	data, err := json.MarshalIndent(c, "", "  ")
	if err != nil {
		return err
	}
	return os.WriteFile(CursorPath(stateDir), data, 0o644)
}

// Status 是 ok wiki status 的结果。Behind=-1 表示 git 不可用。
type Status struct {
	HasWiki    bool   `json:"has_wiki"`
	LastCommit string `json:"last_commit,omitempty"`
	Behind     int    `json:"behind"`
	Stale      bool   `json:"stale"`
	Threshold  int    `json:"threshold"`
}

// CheckStatus 计算 wiki 落后状态。srcDir 为项目源码目录（git 仓库）。
// 无游标时 Behind 为全历史 commit 数；游标 commit 失踪/非 git 项目时 Behind=-1。
func CheckStatus(stateDir, srcDir string, threshold int) *Status {
	st := &Status{Behind: -1, Threshold: threshold}
	c := LoadCursor(stateDir)
	if c == nil {
		if n, err := countCommits(srcDir, "HEAD"); err == nil {
			st.Behind = n
			st.Stale = threshold > 0 && n >= threshold
		}
		return st
	}
	st.HasWiki = true
	st.LastCommit = c.LastCommit
	if c.LastCommit == "" {
		return st
	}
	if n, err := countCommits(srcDir, c.LastCommit+"..HEAD"); err == nil {
		st.Behind = n
		st.Stale = threshold > 0 && n >= threshold
	}
	return st
}

// HeadCommit 返回 srcDir 的 HEAD 完整 hash。
func HeadCommit(srcDir string) (string, error) {
	out, err := exec.Command("git", "-C", srcDir, "rev-parse", "HEAD").Output()
	if err != nil {
		return "", err
	}
	return strings.TrimSpace(string(out)), nil
}

func countCommits(srcDir, rev string) (int, error) {
	out, err := exec.Command("git", "-C", srcDir, "rev-list", "--count", rev).Output()
	if err != nil {
		return 0, err
	}
	return strconv.Atoi(strings.TrimSpace(string(out)))
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/wiki/ -v`
Expected: 5 个测试全 PASS。

- [ ] **Step 5: Commit**

```bash
git add internal/wiki/
git commit -m "feat: wiki package with cursor state and git behind-count"
```

---

### Task 2: config 增加 [wiki] 小节

**Files:**
- Modify: `internal/config/config.go:49-69`
- Test: `internal/config/config_test.go`（若无此文件则创建，仿照同包现有测试风格）

**Interfaces:**
- Consumes: 无。
- Produces: `type Wiki struct { StaleCommits int }`（toml `stale_commits`）；`Config.Wiki Wiki`（toml `wiki`）；`Default().Wiki.StaleCommits == 20`。

- [ ] **Step 1: 写失败测试**

```go
func TestWikiConfigDefaultAndOverride(t *testing.T) {
	def := Default()
	if def.Wiki.StaleCommits != 20 {
		t.Fatalf("default stale_commits = %d, want 20", def.Wiki.StaleCommits)
	}
	dir := t.TempDir()
	global := filepath.Join(dir, "global.toml")
	project := filepath.Join(dir, "project.toml")
	os.WriteFile(global, []byte("[wiki]\nstale_commits = 50\n"), 0o644)
	os.WriteFile(project, []byte("[wiki]\nstale_commits = 0\n"), 0o644)
	cfg, err := LoadMerged(project, global)
	if err != nil {
		t.Fatal(err)
	}
	if cfg.Wiki.StaleCommits != 0 {
		t.Fatalf("project override: %d, want 0", cfg.Wiki.StaleCommits)
	}
	// 项目文件不写 wiki 时继承全局
	os.WriteFile(project, []byte(""), 0o644)
	cfg, _ = LoadMerged(project, global)
	if cfg.Wiki.StaleCommits != 50 {
		t.Fatalf("global inherit: %d, want 50", cfg.Wiki.StaleCommits)
	}
}
```

（确认 `LoadMerged(projectPath, globalPath)` 的实参顺序与现有测试一致——签名见 `config.go:87`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/config/ -run TestWikiConfig -v`
Expected: 编译错误 `cfg.Wiki undefined`。

- [ ] **Step 3: 实现**

在 `config.go` 的 `Capture` 结构体（`config.go:49-52`）后加：

```go
// Wiki 控制 wiki 落后提示。
type Wiki struct {
	StaleCommits int `toml:"stale_commits"` // 落后多少 commit 提示；0 = 关闭
}
```

`Config` 结构体（`config.go:54-60`）加字段：

```go
	Wiki      Wiki          `toml:"wiki"`
```

`Default()`（`config.go:62-69`）的返回字面量里加：

```go
		Wiki:      Wiki{StaleCommits: 20},
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/config/`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add internal/config/
git commit -m "feat: [wiki] stale_commits config with project override"
```

---

### Task 3: index 包 Wiki 目录查询 + INDEX.md 目录节

**Files:**
- Modify: `internal/index/query.go`（`Mandatory()` 之后，约 `query.go:138`）、`internal/index/sync.go:226-248`
- Test: `internal/index/sync_test.go`（若无则在现有 sync 测试文件追加）

**Interfaces:**
- Consumes: entries 表（`db.go:21-30`）。
- Produces:
  - `type WikiEntry struct { Title, Filename, Summary string }`
  - `func (db *DB) WikiEntries() ([]WikiEntry, error)`——`draft=0 AND tags LIKE '%wiki%'`，按 title 排序
  - `func (db *DB) WikiCount() (int, error)`
  - rebuildIndex 输出含"## Wiki 目录"节（无 wiki 条目时省略）

- [ ] **Step 1: 写失败测试**

仿照现有 sync 测试的建库方式（先看 `sync_test.go` 怎么造 entries；一般是写 knowledge/*.md 后 `db.Sync(dir, nil)`）：

```go
func TestRebuildIndexWikiSection(t *testing.T) {
	// 建临时 knowledge 目录：两条 wiki 条目（一条 draft）、一条普通条目
	dir := t.TempDir()
	write := func(name, fm string) {
		if err := os.WriteFile(filepath.Join(dir, name), []byte(fm), 0o644); err != nil {
			t.Fatal(err)
		}
	}
	write("b.md", "---\ntitle: B 模块\ntype: reference\ntags: [wiki, 模块]\nsummary: B 的描述\ndraft: false\nmandatory: false\n---\n正文\n")
	write("a.md", "---\ntitle: A 架构\ntype: reference\ntags: [wiki, 架构]\nsummary: A 的描述\ndraft: false\nmandatory: false\n---\n正文\n")
	write("draft.md", "---\ntitle: 草稿wiki\ntype: reference\ntags: [wiki]\nsummary: 不应出现\ndraft: true\nmandatory: false\n---\n正文\n")
	write("plain.md", "---\ntitle: 普通条目\ntype: note\ntags: [其他]\nsummary: 不进目录\ndraft: false\nmandatory: false\n---\n正文\n")

	db, err := Open(filepath.Join(dir, "kb.db"))
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()
	if err := db.Sync(dir, nil); err != nil {
		t.Fatal(err)
	}

	// WikiEntries：按 title 排序、排除 draft 与非 wiki
	entries, err := db.WikiEntries()
	if err != nil {
		t.Fatal(err)
	}
	if len(entries) != 2 || entries[0].Title != "A 架构" || entries[1].Title != "B 模块" {
		t.Fatalf("WikiEntries: %+v", entries)
	}
	if n, _ := db.WikiCount(); n != 2 {
		t.Fatalf("WikiCount = %d, want 2", n)
	}

	// INDEX.md 含目录节且链接指向文件名
	data, err := os.ReadFile(filepath.Join(filepath.Dir(dir), "INDEX.md"))
	if err != nil {
		// rebuildIndex 写到 <dir>/../INDEX.md（sync.go:248），即 dir 的父目录
		t.Fatal(err)
	}
	s := string(data)
	if !strings.Contains(s, "## Wiki 目录") ||
		!strings.Contains(s, "- [A 架构](a.md) — A 的描述") ||
		strings.Contains(s, "草稿wiki") {
		t.Fatalf("INDEX.md:\n%s", s)
	}
}

func TestRebuildIndexNoWikiSection(t *testing.T) {
	dir := t.TempDir()
	if err := os.WriteFile(filepath.Join(dir, "x.md"), []byte("---\ntitle: X\ntype: note\ntags: []\nsummary: s\ndraft: false\nmandatory: false\n---\n正文\n"), 0o644); err != nil {
		t.Fatal(err)
	}
	db, err := Open(filepath.Join(dir, "kb.db"))
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()
	if err := db.Sync(dir, nil); err != nil {
		t.Fatal(err)
	}
	data, _ := os.ReadFile(filepath.Join(filepath.Dir(dir), "INDEX.md"))
	if strings.Contains(string(data), "Wiki 目录") {
		t.Fatalf("no wiki entries should mean no section:\n%s", data)
	}
}
```

注意：先读 `internal/index/sync_test.go` 确认 `Open`/`Sync` 的实际用法与临时目录惯例，测试代码按现状对齐（上面是基于 `sync.go:52,226-248` 与 `db.go:48` 的事实写的）。

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/index/ -run WikiSection -v`
Expected: 编译错误 `db.WikiEntries undefined`。

- [ ] **Step 3: 实现**

`query.go` 的 `Mandatory()` 之后加：

```go
// WikiEntry 是 Wiki 目录的一行。
type WikiEntry struct {
	Title    string
	Filename string
	Summary  string
}

// WikiEntries 返回打 wiki 标签的已转正条目（按 title 排序）。
func (db *DB) WikiEntries() ([]WikiEntry, error) {
	rows, err := db.db.Query(`SELECT title, filename, summary FROM entries WHERE draft = 0 AND tags LIKE '%wiki%' ORDER BY title`)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []WikiEntry
	for rows.Next() {
		var e WikiEntry
		if err := rows.Scan(&e.Title, &e.Filename, &e.Summary); err != nil {
			return nil, err
		}
		out = append(out, e)
	}
	return out, rows.Err()
}

// WikiCount 返回 wiki 条目数（ok wiki mark 展示用）。
func (db *DB) WikiCount() (int, error) {
	var n int
	err := db.db.QueryRow(`SELECT COUNT(*) FROM entries WHERE draft = 0 AND tags LIKE '%wiki%'`).Scan(&n)
	return n, err
}
```

（`db.db` 的字段名以 `Mandatory()`（`query.go:123`）实际使用的为准。）

`sync.go` 的 `rebuildIndex`（`sync.go:226`）：在条目循环之后、写文件之前加：

```go
	if wikiEntries, err := db.WikiEntries(); err == nil && len(wikiEntries) > 0 {
		b.WriteString("\n## Wiki 目录\n\n")
		for _, we := range wikiEntries {
			if we.Summary != "" {
				fmt.Fprintf(&b, "- [%s](%s) — %s\n", we.Title, we.Filename, we.Summary)
			} else {
				fmt.Fprintf(&b, "- [%s](%s)\n", we.Title, we.Filename)
			}
		}
	}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/index/`
Expected: PASS（含既有测试——无 wiki 条目时输出必须逐字节不变）。

- [ ] **Step 5: Commit**

```bash
git add internal/index/
git commit -m "feat: wiki catalog section in INDEX.md from tagged entries"
```

---

### Task 4: CLI `ok wiki status|mark` + main 接线

**Files:**
- Modify: `internal/cli/cli.go`（CaptureCmd 之后，约 `cli.go:540`）、`cmd/ok/main.go:27-65,98-100`
- Test: `internal/cli/cli_test.go`（追加，仿照现有 cli 测试的 OK_HOME 隔离模式）

**Interfaces:**
- Consumes: Task 1 的 `wiki.CheckStatus/HeadCommit/LoadCursor/SaveCursor/Status`；Task 2 的 `pc.Config.Wiki.StaleCommits`；Task 3 的 `db.WikiCount()`。
- Produces: `func WikiCmd(args []string, stdout, stderr io.Writer) int`；main dispatch `case "wiki"`；usage 字符串含 `wiki`。

- [ ] **Step 1: 写失败测试**

```go
func TestWikiStatusAndMark(t *testing.T) {
	home := t.TempDir()
	t.Setenv("OK_HOME", home)
	t.Setenv("KIMI_CODE_HOME", filepath.Join(home, "kimi"))
	// 在临时 git 仓库里跑（cwd 即项目）
	repo := t.TempDir()
	gitEnv := append(os.Environ(), "GIT_AUTHOR_NAME=t", "GIT_AUTHOR_EMAIL=t@t",
		"GIT_COMMITTER_NAME=t", "GIT_COMMITTER_EMAIL=t@t")
	git := func(args ...string) {
		cmd := exec.Command("git", args...)
		cmd.Dir = repo
		cmd.Env = gitEnv
		if out, err := cmd.CombinedOutput(); err != nil {
			t.Fatalf("git %v: %v\n%s", args, err, out)
		}
	}
	git("init")
	os.WriteFile(filepath.Join(repo, "f"), []byte("x"), 0o644)
	git("add", ".")
	git("commit", "-m", "c1")
	// 切 cwd 到 repo（cli 走 resolveFromCwd）
	old, _ := os.Getwd()
	defer os.Chdir(old)
	os.Chdir(repo)

	var out, errb strings.Builder
	if code := Init(nil, &out, &errb); code != 0 {
		t.Fatalf("init: %d %s", code, errb.String())
	}
	out.Reset()
	if code := WikiCmd([]string{"status"}, &out, &errb); code != 0 {
		t.Fatalf("status: %d %s", code, errb.String())
	}
	var st struct {
		Project  string `json:"project"`
		HasWiki  bool   `json:"has_wiki"`
		Behind   int    `json:"behind"`
		Stale    bool   `json:"stale"`
		Threshold int   `json:"threshold"`
	}
	if err := json.Unmarshal([]byte(out.String()), &st); err != nil {
		t.Fatalf("status output %q: %v", out.String(), err)
	}
	if st.HasWiki || st.Behind != 1 || st.Threshold != 20 {
		t.Fatalf("status: %+v", st)
	}

	// mark 无参 → 取 HEAD
	out.Reset()
	if code := WikiCmd([]string{"mark"}, &out, &errb); code != 0 {
		t.Fatalf("mark: %d %s", code, errb.String())
	}
	if !strings.Contains(out.String(), "已记录 wiki 游标") {
		t.Fatalf("mark output: %q", out.String())
	}
	out.Reset()
	WikiCmd([]string{"status"}, &out, &errb)
	json.Unmarshal([]byte(out.String()), &st)
	if !st.HasWiki || st.Behind != 0 {
		t.Fatalf("after mark: %+v", st)
	}
}
```

（先读 `cli_test.go` 现有测试，确认 `Init` 的调用方式与 `resolveFromCwd` 对 cwd 的依赖；按现状对齐。）

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/cli/ -run TestWikiStatusAndMark -v`
Expected: 编译错误 `WikiCmd undefined`。

- [ ] **Step 3: 实现**

`cli.go` 的 `CaptureCmd` 之后加（prologue 照抄 `Add`（`cli.go:103-113`）的 `resolveFromCwd` 用法；`fs.Arg(0)` 子动作模式照抄 `CaptureCmd`（`cli.go:490`））：

```go
// WikiCmd 处理 ok wiki status|mark。
func WikiCmd(args []string, stdout, stderr io.Writer) int {
	fs := flag.NewFlagSet("wiki", flag.ContinueOnError)
	fs.SetOutput(stderr)
	if err := fs.Parse(args); err != nil {
		return 2
	}
	name, pc, code := resolveFromCwd(stderr) // 以 Add 的实际 prologue 为准
	if code != 0 {
		return code
	}
	cwd, _ := os.Getwd()

	switch fs.Arg(0) {
	case "status":
		st := wiki.CheckStatus(pc.Store.StateDir(), cwd, pc.Config.Wiki.StaleCommits)
		out := map[string]any{
			"project":     name,
			"has_wiki":    st.HasWiki,
			"behind":      st.Behind,
			"stale":       st.Stale,
			"threshold":   st.Threshold,
		}
		if st.LastCommit != "" {
			out["last_commit"] = st.LastCommit
		}
		_ = json.NewEncoder(stdout).Encode(out)
		return 0
	case "mark":
		commit := fs.Arg(1)
		if commit == "" {
			commit, _ = wiki.HeadCommit(cwd) // 非 git 项目留空，只写时间戳
		}
		count := 0
		if db, err := index.Open(pc.Store.KbPath()); err == nil {
			if err := db.Sync(pc.Store.KnowledgeDir(), nil); err == nil {
				count, _ = db.WikiCount()
			}
			db.Close()
		}
		c := &wiki.Cursor{LastCommit: commit, GeneratedAt: time.Now(), EntryCount: count}
		if err := wiki.SaveCursor(pc.Store.StateDir(), c); err != nil {
			fmt.Fprintln(stderr, "写游标失败:", err)
			return 1
		}
		short := commit
		if len(short) > 7 {
			short = short[:7]
		}
		if short == "" {
			short = "(无 git)"
		}
		fmt.Fprintf(stdout, "已记录 wiki 游标 %s（%d 条 wiki 条目）\n", short, count)
		return 0
	default:
		fmt.Fprintln(stderr, "用法: ok wiki <status|mark [commit]>")
		return 2
	}
}
```

`main.go` dispatch（`case "capture"` 后）加：

```go
	case "wiki":    return cli.WikiCmd(argv[2:], os.Stdout, os.Stderr)
```

usage（`main.go:99`）改为：

```go
	fmt.Fprintln(os.Stderr, "用法: ok [gui] <setup|init|add|propose|approve|capture|wiki|search|index|list|doctor|on|off|hook> ...")
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/cli/ ./cmd/ok/`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add internal/cli/ cmd/ok/
git commit -m "feat: ok wiki status/mark subcommands"
```

---

### Task 5: prompt hook wiki 落后提示

**Files:**
- Modify: `internal/state/state.go:11-18`、`internal/hook/hook.go:186-189`（截断与输出处）
- Test: `internal/hook/hook_test.go`（追加）

**Interfaces:**
- Consumes: Task 1 `wiki.CheckStatus`；Task 2 `pc.Config.Wiki.StaleCommits`。
- Produces: `state.Session.WikiNudged bool`（json `wiki_nudged`）；`HandlePrompt` 输出末尾在条件满足时附提示文案。

- [ ] **Step 1: 写失败测试**

仿照 `hook_test.go` 现有 prompt 测试的构造（payload、OK_HOME、项目注册），核心断言：

```go
func TestPromptWikiNudge(t *testing.T) {
	// 环境：临时 OK_HOME；临时 git 仓库 2 个 commit；ok init 等价注册；
	// 项目 config.toml 写 [wiki]\nstale_commits = 1
	// payload: {"hook_event_name":"UserPromptSubmit","session_id":"s1","cwd":<repo>,"prompt":"hello"}
	// 第一次 HandlePrompt：输出末尾含 "本项目还没有 wiki"
	// 第二次 HandlePrompt（同 session）：不再含提示
	// 另起一个项目 config 写 stale_commits = 0：不含提示
}
```

（先读 `hook_test.go` 现有 `HandlePrompt` 测试，复用其项目注册/构造 payload 的 helper；断言文案与 Step 3 实现逐字一致。另加一个用例：非 git 目录（`t.TempDir()` 做 cwd）输出不含提示且退出码不变。）

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/hook/ -run TestPromptWikiNudge -v`
Expected: FAIL（输出无提示文案）。

- [ ] **Step 3: 实现**

`state.go` 的 `Session`（`state.go:11-18`）加字段：

```go
	WikiNudged          bool     `json:"wiki_nudged"`
```

`hook.go`：在 `out := store.TruncateToBudget(...)`（`hook.go:186`）之后、`fmt.Fprintln(w, out)`（`hook.go:189`）之前插入（`ev` 为已解析的事件结构体，cwd 字段名以 `hook.go` 实际解析结果为准；`pc`/`st` 为该函数内已有的项目上下文与 session 变量）：

```go
	if nudge := wikiNudge(pc, st, ev.Cwd); nudge != "" {
		out += nudge
	}
```

`hook.go` 文件末尾新增（imports 加 `openknowledge/internal/wiki`）：

```go
// wikiNudge 返回 wiki 落后提示（每会话最多一次，预算外放行）；不适用返回空串。
// fail-open：git 不可用/非 git 项目时 CheckStatus 不 stale，自然无提示。
func wikiNudge(pc *projectCtx, st *state.Session, cwd string) string {
	threshold := pc.Config.Wiki.StaleCommits
	if threshold <= 0 || st.WikiNudged {
		return ""
	}
	s := wiki.CheckStatus(pc.Store.StateDir(), cwd, threshold)
	if !s.Stale {
		return ""
	}
	st.WikiNudged = true
	_ = st.Save(pc.Store.StateDir())
	if !s.HasWiki {
		return "\n[OpenKnowledge] 本项目还没有 wiki，建议用 openknowledge-wiki 技能生成项目 wiki（含架构、模块与演进历史）。\n"
	}
	return fmt.Sprintf("\n[OpenKnowledge] wiki 已落后 %d 个 commit，建议用 openknowledge-wiki 技能增量更新。\n", s.Behind)
}
```

（`projectCtx` 替换为 `hook.go` 中 `pc` 的真实类型名。）

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/hook/ ./internal/state/`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add internal/hook/ internal/state/
git commit -m "feat: prompt hook nudges once per session when wiki is stale"
```

---

### Task 6: openknowledge-wiki 技能文件 + go:embed 分发

**Files:**
- Create: `internal/setupx/skills/openknowledge-wiki/SKILL.md`
- Modify: `internal/setupx/setupx.go:159-172,226-232`
- Test: `internal/setupx/setupx_test.go:149-164`

**Interfaces:**
- Consumes: 现有 `skillTemplates`/`InstallSkills` 机制。
- Produces: `skillTemplates["openknowledge-wiki"]`（embed 载入）；安装后 `~/.agents/skills/openknowledge-wiki/SKILL.md`。

注意：spec 写的是仓库根 `skills/` 目录，但 `go:embed` 只能嵌包内文件，故实际位置为 `internal/setupx/skills/openknowledge-wiki/SKILL.md`。

- [ ] **Step 1: 写失败测试**

`setupx_test.go` 的 `TestInstallSkills`（`setupx_test.go:149-164`）技能名单切片加 `"openknowledge-wiki"`：

```go
	for _, name := range []string{"openknowledge-init", "openknowledge-on", "openknowledge-off", "openknowledge-propose", "openknowledge-capture", "openknowledge-wiki"} {
```

并追加一个内容断言测试：

```go
func TestInstallWikiSkillContent(t *testing.T) {
	dir := t.TempDir()
	t.Setenv("OK_SKILLS_HOME", dir)
	if err := InstallSkills(`D:\bin\ok.exe`); err != nil {
		t.Fatal(err)
	}
	data, err := os.ReadFile(filepath.Join(dir, "openknowledge-wiki", "SKILL.md"))
	if err != nil {
		t.Fatal(err)
	}
	s := string(data)
	for _, want := range []string{"name: openknowledge-wiki", "wiki status", "wiki mark", "D:/bin/ok.exe", "type: reference", "wiki"} {
		if !strings.Contains(s, want) {
			t.Fatalf("skill missing %q", want)
		}
	}
	if strings.Contains(s, "{{EXE}}") {
		t.Fatal("exe placeholder not baked")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/setupx/ -run TestInstall -v`
Expected: FAIL（openknowledge-wiki/SKILL.md 不存在）。

- [ ] **Step 3: 实现**

创建 `internal/setupx/skills/openknowledge-wiki/SKILL.md`，内容完整如下：

````markdown
---
name: openknowledge-wiki
description: 为已存在的项目生成或增量更新 OpenKnowledge 项目 wiki——扫描代码结构与 git 历史，经 ok add 录入 reference 条目（tags 含 wiki），条目随混合检索注入，目录固定进 INDEX.md。当用户要求"生成项目 wiki""初始化 wiki""更新 wiki""把项目沉淀成 wiki"时使用。
---

# openknowledge-wiki

把当前项目沉淀成 wiki：架构总览 + 模块条目 + 演进里程碑。全部条目用 Bash 调 `"{{EXE}}"` 写入，直接转正（非草稿），用户在 GUI 可随时修订。

## 第 0 步：状态检查

```bash
"{{EXE}}" wiki status
```

输出 JSON：`has_wiki`（是否生成过）、`last_commit`（游标）、`behind`（落后 commit 数，-1 = 非 git 项目）、`stale`、`threshold`。

- `has_wiki:false` → 走【全量流程】
- `has_wiki:true` 且 `behind > 0` → 走【增量流程】
- `has_wiki:true` 且 `behind = 0` → 告诉用户 wiki 已是最新，结束

## 全量流程

1. **盘点**：读目录结构（跳过 `.git`、`node_modules`、`vendor`、`dist`、`target` 等产物目录）、README、构建文件（go.mod / package.json / pom.xml 等）。文件数 > 500 时先向用户报告规模，按顶层目录分层扫描，不要试图读完全部代码。
2. **读关键代码**：每个顶层模块读入口文件与核心类型，理解职责与模块间依赖。
3. **消化历史**（`behind:-1` 时跳过此步）：
   ```bash
   git log --oneline | head -200
   git log --reverse --oneline | head -20   # 项目起源
   ```
   识别里程碑：版本号变更、大重构、目录结构突变、重要功能引入。只提炼转折点，不逐 commit 叙述。
4. **写条目**（规范见下）：第一条固定是"架构总览"，然后每模块/每主题一条，最后一条"演进历程"记里程碑。
5. **推进游标**：
   ```bash
   "{{EXE}}" wiki mark
   ```
6. **汇报**：列出写入的条目清单（标题 + 一句话），告诉用户可在 GUI 修订。

## 增量流程

1. 取游标范围内的变更：
   ```bash
   git log --oneline <last_commit>..HEAD
   git diff --stat <last_commit>..HEAD
   ```
2. 只消化这段变更：受影响模块的旧条目用同名 `add` 重写覆盖；新主题新增条目；"演进历程"条目追加新里程碑后重写。
3. `"{{EXE}}" wiki mark` 推进游标，汇报变更摘要。

## 条目规范（每条都必须遵守）

- 正文先写入临时 .md 文件，再执行：
  ```bash
  "{{EXE}}" add --title <标题> --type reference --tags wiki,<主题tag> --summary <一句话> --file <正文.md>
  ```
- `type` 固定 `reference`；tags 第一个固定 `wiki`，第二个是主题（如 `架构`、`daemon`、`安装器`）。
- `summary` 必填一句话——它会出现在 INDEX.md 的 Wiki 目录里。
- 一个主题一条目，正文 300~800 字；标题即文件名（同名覆盖，用于增量更新）。
- 内容写"为什么"和"怎么协作"，不抄代码；关键文件用 `path:行号` 引用。

## 护栏

- 不读 `.env`、密钥、证书等敏感文件。
- 不用向用户追问项目名——`wiki status` 与 `add` 自动按当前目录路由项目。
- 全部失败可重入：重复执行本技能不会产生重复条目（同名覆盖）。
````

`setupx.go` 修改：

文件头 import 加 `_ "embed"`，在 `skillTemplates`（`setupx.go:226`）之前加：

```go
//go:embed skills/openknowledge-wiki/SKILL.md
var wikiSkillTemplate string

func init() {
	skillTemplates["openknowledge-wiki"] = wikiSkillTemplate
}
```

（`InstallSkills`（`setupx.go:160-172`）无需改动——它遍历同一张 map。）

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/setupx/`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add internal/setupx/
git commit -m "feat: openknowledge-wiki skill shipped via go:embed"
```

---

### Task 7: 端到端测试 + 文档

**Files:**
- Create: `cmd/ok/wiki_test.go`、`docs/changelogs/2026-07-28-wiki.md`
- Modify: `README.md`（技能数量与列表）、`docs/ARCHITECTURE.md`（6.x 数据流 + 技能清单小节）

**Interfaces:**
- Consumes: 前 6 个任务的全部产物。
- Produces: 无（终态）。

- [ ] **Step 1: 写端到端测试**

`cmd/ok/wiki_test.go`（与 `integration_test.go` 同包，复用其 `runOK` helper 与二进制构建模式——先读 `integration_test.go:20-70` 对齐；注意照 `integration_test.go:65` 在测试末尾 `daemon stop`）：

```go
func TestWikiEndToEnd(t *testing.T) {
	home := t.TempDir()   // OK_HOME
	proj := t.TempDir()   // git 项目
	// git init + 3 commits（GIT_AUTHOR/COMMITTER env，同 Task 1 模式）
	// runOK(t, home, proj, "", "init")
	// status：has_wiki=false behind=3
	// runOK "add" 两条 wiki 条目（--type reference --tags wiki,x --summary ...）
	// runOK "wiki" "mark"
	// 断言 <OK_HOME>/projects/<名>/INDEX.md 含 "## Wiki 目录"
	// 追加 1 个 commit
	// status：behind=1
	// 项目 config.toml 追加 [wiki]\nstale_commits = 1
	// runOK hook prompt（payload 带 session_id 与 cwd=proj）：stdout 含 "落后 1 个 commit"
	// 同 session 第二次 prompt：不再含提示
	// 末尾 runOK "daemon" "stop"
}
```

- [ ] **Step 2: 跑测试确认通过**

Run: `go test ./cmd/ok/ -run TestWikiEndToEnd -v`
Expected: PASS。

- [ ] **Step 3: 文档**

`docs/changelogs/2026-07-28-wiki.md`：仿照 `docs/changelogs/2026-07-23-web-gui.md` 的格式，写清——`ok wiki status/mark`、`[wiki] stale_commits`（默认 20）、INDEX.md Wiki 目录、prompt hook 每会话一次提示、`openknowledge-wiki` 技能（第 6 个技能）。

`README.md`：技能清单从 5 个改 6 个，加 `openknowledge-wiki` 一行说明；如命令清单缺 `wiki`，补上。

`docs/ARCHITECTURE.md`：

- §6 数据流加一小节"6.x wiki：游标（state/wiki.json）→ wiki status 落后计数 → INDEX.md Wiki 目录（tags LIKE '%wiki%' 且 draft=0）→ prompt hook 每会话一次 nudge（session.WikiNudged）"。
- 技能清单加 `openknowledge-wiki`。
- §8 存储层补 `state/wiki.json`（不受 session 7 天 GC 影响）。

- [ ] **Step 4: 全量验证**

Run: `go build ./... && go vet ./... && go test ./...`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add cmd/ok/wiki_test.go docs/ README.md
git commit -m "test+docs: wiki end-to-end coverage, changelog and architecture updates"
```

---

## Self-Review 记录

- spec 覆盖：§2 CLI→Task 4；§3 config→Task 2；§4 游标/目录→Task 1/3；§5 hook nudge→Task 5；§6 技能→Task 6；§7 错误处理→Task 1（CheckStatus 三分支）+各任务 fail-open；§8 测试→各 Task Step 1 + Task 7 e2e。全覆盖。
- 偏差记录：spec 中技能路径 `skills/` 改为 `internal/setupx/skills/`（go:embed 包内约束），已在 Task 6 注明。
- 类型一致性：`CheckStatus(stateDir, srcDir, threshold)`、`SaveCursor(stateDir, c)`、`WikiEntries()/WikiCount()`、`WikiCmd(args, stdout, stderr)` 在消费任务中拼写一致。
