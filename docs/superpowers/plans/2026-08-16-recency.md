# 时效信号（v2.18.0）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给检索注入加时效信号——按条目 mtime 新鲜度给融合分乘系数（fresh 内 1.0、stale 后 floor 0.85、中间线性，按条目类型分窗），陈旧条目在近似同分时让位，但不影响准入。

**Architecture:** `internal/index/recency.go` 新增两个纯函数：`Factor`（单条目系数）与 `applyRecency`（对准入集合乘系数并返回因之改变名次的条目清单）；`queryAll` 在融合（RRF/weighted 均在）之后、尾部排序之前调用。`entries.mtime`（文件系统 mtime，Unix 秒，sync 已维护）经两条 SELECT 加列进 `Hit.Mtime`。观测走 QueryInfo 新字段 `RecencyShifted`，hook 记 ok.log、ok search 打 stderr（照抄 SemanticRejected 链路）。配置 `[retrieve.recency]` + `[retrieve.recency.windows]` 子表。

**Tech Stack:** Go（标准库 only）、TOML 配置（BurntSushi/toml v1.6.0）。

**Spec:** `docs/superpowers/specs/2026-08-16-retrieval-evolution.md` §4（特性②）。

## Global Constraints

- **时效不参与准入**：陈旧不等于无关——系数只在融合分之后乘、排序之前生效；准入判定（MinScoreFloor/SemanticFloor）一行不动。
- **fail-open**：类型未知、窗口全零/非法、mtime<=0、floor 非法——一律返回 1.0（不衰减）；观测链路出错仅记 ok.log。
- **mtime = 文件系统 mtime（Unix 秒）**，与 frontmatter `created`/`updated` 无关（entry.Parse 非严格解码，那两个键是死字段）；编辑条目即刷新新鲜度是 feature；批量重格式化/迁移会整体刷新——changelog 须注明。
- 两种融合模式（rrf / weighted）都乘时效系数。
- 窗口配置用 `[]int`（不用 `[2]int`——toml 定长数组解码长度不符会让整个 Load 报错，违反 fail-open）；`len != 2` 按不衰减。
- 测试风格：标准库 testing + t.TempDir()，非表驱动、不用 testify；构造陈旧条目用 `os.Chtimes` 设过去时间再 Sync（先例 sync_fastpath_test.go:72-79）。
- GUI 暴露 recency 配置是**非目标**（spec §2，跟进项）；本 plan 不动 web/。
- 本 plan 不含版本号 bump / sync-version / dist 同步——发布时例行处理。

---

### Task 1: config 增 `Retrieve.Recency` 子表 + 默认值

**Files:**
- Modify: `internal/config/config.go`（Retrieve 结构体 :107-135 区域、Default :184-194 区域）
- Test: `internal/config/config_test.go`

**Interfaces:**
- Consumes: 无
- Produces（Task 2/3 依赖，不得改）：
  - `type RetrieveRecency struct { Enabled bool; Floor float64; Windows RecencyWindows }`（toml `enabled`/`floor`/`windows`）
  - `type RecencyWindows struct { Rule, Pitfall, Note, Reference []int }`（toml 键同名小写）
  - `Retrieve.Recency RetrieveRecency`（toml `recency`）
  - 默认：Enabled=true、Floor=0.85、Rule/Reference=[180,730]、Pitfall=[90,365]、Note=[60,180]

- [ ] **Step 1: 写失败测试**（追加到 `internal/config/config_test.go`，模板照抄 `TestFusionConfigDefaultAndOverride`）

```go
func TestRecencyConfigDefaultAndOverride(t *testing.T) {
	dir := t.TempDir()
	global := filepath.Join(dir, "global.toml")
	project := filepath.Join(dir, "project.toml")
	// 全缺省：enabled、floor 0.85、四类型窗口
	cfg, err := LoadMerged(project, global)
	if err != nil {
		t.Fatal(err)
	}
	r := cfg.Retrieve.Recency
	if !r.Enabled || r.Floor != 0.85 {
		t.Fatalf("unexpected defaults %+v", r)
	}
	if len(r.Windows.Note) != 2 || r.Windows.Note[0] != 60 || r.Windows.Note[1] != 180 {
		t.Fatalf("unexpected note window %+v", r.Windows.Note)
	}
	if len(r.Windows.Pitfall) != 2 || r.Windows.Pitfall[0] != 90 || r.Windows.Pitfall[1] != 365 {
		t.Fatalf("unexpected pitfall window %+v", r.Windows.Pitfall)
	}
	// 全局覆盖：floor + note 窗口；其余窗口继承默认
	if err := os.WriteFile(global, []byte("[retrieve.recency]\nfloor = 0.7\n[retrieve.recency.windows]\nnote = [10, 20]\n"), 0o644); err != nil {
		t.Fatal(err)
	}
	cfg, err = LoadMerged(project, global)
	if err != nil {
		t.Fatal(err)
	}
	r = cfg.Retrieve.Recency
	if r.Floor != 0.7 || r.Windows.Note[0] != 10 || r.Windows.Note[1] != 20 {
		t.Fatalf("global override failed %+v", r)
	}
	if r.Windows.Rule[1] != 730 {
		t.Fatalf("未覆盖窗口应继承默认 %+v", r.Windows.Rule)
	}
	// 项目显式关闭
	if err := os.WriteFile(project, []byte("[retrieve.recency]\nenabled = false\n"), 0o644); err != nil {
		t.Fatal(err)
	}
	cfg, err = LoadMerged(project, global)
	if err != nil {
		t.Fatal(err)
	}
	if cfg.Retrieve.Recency.Enabled {
		t.Fatalf("project override failed %+v", cfg.Retrieve.Recency)
	}
	// 项目清掉 → 重新继承（全局未设 enabled → 回默认 true）
	if err := os.WriteFile(project, []byte(""), 0o644); err != nil {
		t.Fatal(err)
	}
	cfg, err = LoadMerged(project, global)
	if err != nil {
		t.Fatal(err)
	}
	if !cfg.Retrieve.Recency.Enabled || cfg.Retrieve.Recency.Floor != 0.7 {
		t.Fatalf("project 缺键应继承 %+v", cfg.Retrieve.Recency)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/config/ -run TestRecencyConfig -v`
Expected: FAIL（`cfg.Retrieve.Recency undefined`）

- [ ] **Step 3: 实现**

`internal/config/config.go`，`RetrieveGate` 类型之后加两个新类型：

```go
// RetrieveRecency 控制时效信号（[retrieve.recency] 子表）：按条目 mtime 新鲜度
// 给融合分乘系数（不参与准入）——age ≤ fresh → 1.0，age ≥ stale → floor，
// 中间线性。Floor 非法（<=0 或 >1）按 0.85。
type RetrieveRecency struct {
	Enabled bool           `toml:"enabled"` // 默认 true（见 Default）
	Floor   float64        `toml:"floor"`   // 陈旧系数下限，默认 0.85
	Windows RecencyWindows `toml:"windows"`
}

// RecencyWindows 按条目类型的 [fresh_days, stale_days] 窗口（天）。全零、
// 长度不为 2 或 stale<=fresh 均视为该类型不衰减（fail-open）。
type RecencyWindows struct {
	Rule      []int `toml:"rule"`
	Pitfall   []int `toml:"pitfall"`
	Note      []int `toml:"note"`
	Reference []int `toml:"reference"`
}
```

`Retrieve` 结构体 `Gate` 字段之后加：

```go
	// Recency 是时效信号（[retrieve.recency] 子表）：陈旧条目在近似同分时让位，
	// 不参与准入。编辑条目即刷新 mtime 新鲜度（feature）。
	Recency RetrieveRecency `toml:"recency"`
```

`Default()` 的 Retrieve 字面量改为：

```go
		Retrieve:   Retrieve{Alpha: 1.0, Beta: 1.0, TopN: 2, MinScore: 0.5, MinGap: 0.25, Fusion: "rrf", RrfK: 60,
			Gate:    RetrieveGate{Enabled: true},
			Recency: RetrieveRecency{Enabled: true, Floor: 0.85, Windows: RecencyWindows{
				Rule: []int{180, 730}, Pitfall: []int{90, 365}, Note: []int{60, 180}, Reference: []int{180, 730},
			}}},
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/config/ -v`
Expected: PASS（含既有测试全部绿）

- [ ] **Step 5: Commit**

```bash
git add internal/config/config.go internal/config/config_test.go
git commit -m "feat(config): [retrieve.recency] 时效信号配置（enabled/floor/分类型窗口）"
```

---

### Task 2: `recency.Factor` + `applyRecency` 纯函数

**Files:**
- Create: `internal/index/recency.go`
- Test: `internal/index/recency_test.go`

**Interfaces:**
- Consumes: `config.RetrieveRecency`（Task 1）、`Hit`（query.go:77-85，本任务暂不改它——applyRecency 先用现有字段，Mtime 在 Task 3 加；**注意**：本任务的 applyRecency 代码引用 `h.Mtime`，若 Task 3 未合入则编译失败——两任务可同分支顺序执行，本任务允许 RED 阶段编译失败、实现阶段先临时给 Hit 加 `Mtime int64` 字段（Task 3 会用它），或把 Hit.Mtime 挪到本任务加。**裁决：Hit.Mtime 字段由本任务加**，Task 3 只做 SELECT/Scan 接线）
- Produces（Task 3/4 依赖，不得改）：
  - `func Factor(typ string, mtime, now int64, cfg config.RetrieveRecency) float64`
  - `func applyRecency(hits map[string]*Hit, now int64, cfg config.RetrieveRecency) []string` — 乘系数（就地改 Score），返回因系数名次变差的条目（`"filename×0.85"` 格式，按新名次排序）；Enabled=false 或无变化返回 nil
  - `Hit.Mtime int64` 字段

- [ ] **Step 1: 写失败测试**

```go
package index

import (
	"math"
	"testing"

	"openknowledge/internal/config"
)

var recencyCfg = config.RetrieveRecency{Enabled: true, Floor: 0.85, Windows: config.RecencyWindows{
	Rule: []int{180, 730}, Pitfall: []int{90, 365}, Note: []int{60, 180}, Reference: []int{180, 730},
}}

const daySec int64 = 86400

func TestFactorBoundaries(t *testing.T) {
	const now int64 = 1_800_000_000
	cases := []struct {
		typ  string
		ageD int64
		want float64
	}{
		{"note", 0, 1.0},     // fresh 内
		{"note", 60, 1.0},    // fresh 边界（age<=fresh → 1.0）
		{"note", 120, 0.925}, // 线性中点：1-0.15*(120-60)/(180-60)
		{"note", 180, 0.85},  // stale 边界
		{"note", 9999, 0.85}, // 远超 stale
		{"pitfall", 90, 1.0},
		{"pitfall", 365, 0.85},
		{"rule", 180, 1.0},
		{"rule", 730, 0.85},
		{"reference", 400, 1.0 - 0.15*220.0/550.0}, // rule 线性段
		{"wiki", 9999, 1.0},                        // 未知类型不衰减
	}
	for _, c := range cases {
		got := Factor(c.typ, now-c.ageD*daySec, now, recencyCfg)
		if math.Abs(got-c.want) > 1e-9 {
			t.Errorf("Factor(%s, age=%dd) = %v, want %v", c.typ, c.ageD, got, c.want)
		}
	}
}

func TestFactorFailOpen(t *testing.T) {
	const now int64 = 1_800_000_000
	old := now - 9999*daySec
	// mtime<=0
	if got := Factor("note", 0, now, recencyCfg); got != 1.0 {
		t.Errorf("mtime=0 应不衰减, got %v", got)
	}
	// floor 非法 → 按 0.85
	bad := recencyCfg
	bad.Floor = 0
	if got := Factor("note", old, now, bad); got != 0.85 {
		t.Errorf("floor=0 应按 0.85, got %v", got)
	}
	bad.Floor = 1.5
	if got := Factor("note", old, now, bad); got != 0.85 {
		t.Errorf("floor=1.5 应按 0.85, got %v", got)
	}
	// 窗口全零 / 长度不对 / stale<=fresh → 不衰减
	for _, w := range [][]int{{0, 0}, {60}, {60, 180, 1}, {180, 60}} {
		c := recencyCfg
		c.Windows.Note = w
		if got := Factor("note", old, now, c); got != 1.0 {
			t.Errorf("非法窗口 %v 应不衰减, got %v", w, got)
		}
	}
}

func TestApplyRecency(t *testing.T) {
	const now int64 = 1_800_000_000
	mk := func() map[string]*Hit {
		return map[string]*Hit{
			"old.md": {Filename: "old.md", Title: "旧", Type: "note", Score: 0.5, Mtime: now - 400*daySec},
			"new.md": {Filename: "new.md", Title: "新", Type: "note", Score: 0.4, Mtime: now},
		}
	}
	// 翻名次：old 0.5×0.85=0.425 仍 > new 0.4？不——0.425>0.4 不翻。
	// 调整：old 0.45×0.85=0.3825 < 0.4 → 翻
	hits := mk()
	hits["old.md"].Score = 0.45
	shifted := applyRecency(hits, now, recencyCfg)
	if hits["old.md"].Score < 0.38 || hits["old.md"].Score > 0.39 {
		t.Errorf("old.md 应乘 0.85: %v", hits["old.md"].Score)
	}
	if len(shifted) != 1 || shifted[0] != "old.md×0.85" {
		t.Errorf("old.md 名次应变差并上榜: %v", shifted)
	}
	// 不翻名次：old 领先够多 → 系数照乘但不上榜
	hits2 := mk()
	if got := applyRecency(hits2, now, recencyCfg); got != nil {
		t.Errorf("名次未变应返回 nil: %v", got)
	}
	if hits2["old.md"].Score != 0.5*0.85 {
		t.Errorf("系数仍应乘: %v", hits2["old.md"].Score)
	}
	// 关闭 → 不动
	hits3 := mk()
	off := recencyCfg
	off.Enabled = false
	if got := applyRecency(hits3, now, off); got != nil || hits3["old.md"].Score != 0.5 {
		t.Errorf("Enabled=false 不应动分数: %v %v", got, hits3["old.md"].Score)
	}
}
```

（注释里"0.425>0.4 不翻"是设计演算，实现者照抄即可——第一组用 0.45 构造翻名次，第二组 mk() 默认 0.5/0.4 构造不翻。）

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/index/ -run 'TestFactor|TestApplyRecency' -v`
Expected: FAIL（`undefined: Factor`）

- [ ] **Step 3: 实现**

`internal/index/query.go` 的 Hit 结构体加字段（本任务裁决由此加）：

```go
type Hit struct {
	Filename string
	Title    string
	Type     string
	Summary  string
	Body     string
	Tags     []string // 供注入层按分支过滤
	Score    float64
	Mtime    int64 // 文件系统 mtime（Unix 秒），时效信号用
}
```

新建 `internal/index/recency.go`：

```go
package index

import (
	"fmt"
	"sort"

	"openknowledge/internal/config"
)

const daySeconds int64 = 86400

// Factor 返回条目的时效系数：age ≤ fresh_days → 1.0；age ≥ stale_days → floor；
// 中间线性过渡。陈旧不等于无关——系数只在融合分之后乘（不参与准入），
// 让陈旧条目在近似同分时让位。
// 以下情形一律返回 1.0（fail-open 不衰减）：类型未知、窗口全零/长度不对/
// stale<=fresh、mtime<=0。floor 非法（<=0 或 >1）按默认 0.85。
func Factor(typ string, mtime, now int64, cfg config.RetrieveRecency) float64 {
	floor := cfg.Floor
	if floor <= 0 || floor > 1 {
		floor = 0.85
	}
	if mtime <= 0 {
		return 1.0
	}
	var w []int
	switch typ {
	case "rule":
		w = cfg.Windows.Rule
	case "pitfall":
		w = cfg.Windows.Pitfall
	case "note":
		w = cfg.Windows.Note
	case "reference":
		w = cfg.Windows.Reference
	default:
		return 1.0
	}
	if len(w) != 2 || w[1] <= w[0] || (w[0] <= 0 && w[1] <= 0) {
		return 1.0
	}
	age := (now - mtime) / daySeconds
	if age <= int64(w[0]) {
		return 1.0
	}
	if age >= int64(w[1]) {
		return floor
	}
	return 1.0 - (1.0-floor)*float64(age-int64(w[0]))/float64(w[1]-w[0])
}

// applyRecency 对准入集合逐条乘时效系数（就地改 Score），返回因系数名次变差
// 的条目（"filename×0.85"，按新名次排序），供 QueryInfo 观测。Enabled=false
// 或无条目受影响返回 nil。
func applyRecency(hits map[string]*Hit, now int64, cfg config.RetrieveRecency) []string {
	if !cfg.Enabled {
		return nil
	}
	byScore := func(hs []*Hit) {
		sort.Slice(hs, func(i, j int) bool {
			if hs[i].Score != hs[j].Score {
				return hs[i].Score > hs[j].Score
			}
			return hs[i].Title < hs[j].Title
		})
	}
	pre := make([]*Hit, 0, len(hits))
	for _, h := range hits {
		pre = append(pre, h)
	}
	byScore(pre)
	preRank := make(map[string]int, len(pre))
	for i, h := range pre {
		preRank[h.Filename] = i
	}
	demoted := map[string]float64{}
	for _, h := range hits {
		if f := Factor(h.Type, h.Mtime, now, cfg); f < 1 {
			h.Score *= f
			demoted[h.Filename] = f
		}
	}
	if len(demoted) == 0 {
		return nil
	}
	post := make([]*Hit, 0, len(hits))
	for _, h := range hits {
		post = append(post, h)
	}
	byScore(post)
	var shifted []string
	for i, h := range post {
		f, ok := demoted[h.Filename]
		if ok && i > preRank[h.Filename] {
			shifted = append(shifted, fmt.Sprintf("%s×%.2f", h.Filename, f))
		}
	}
	return shifted
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/index/ -v`
Expected: PASS（含既有全部绿——Hit 加字段不影响既有构造）

- [ ] **Step 5: Commit**

```bash
git add internal/index/recency.go internal/index/recency_test.go internal/index/query.go
git commit -m "feat(index): 时效系数 Factor + applyRecency 纯函数（Hit 增 Mtime）"
```

---

### Task 3: queryAll 接线——Mtime 取数 + 乘系数 + QueryInfo 观测字段

**Files:**
- Modify: `internal/index/query.go`（QueryInfo :65-74、关键词 SELECT/Scan :167-194、语义 SELECT/Scan :206-234、尾部 :276-293）
- Test: `internal/index/query_recency_test.go`（新建）

**Interfaces:**
- Consumes: `Factor` / `applyRecency` / `Hit.Mtime`（Task 2）、`config.Retrieve.Recency`（Task 1）
- Produces（Task 4 依赖）：
  - `QueryInfo.RecencyShifted []string` — 因时效系数名次变差的条目（`"filename×0.85"`）
  - 行为契约：recency 开启时 hit.Score 已乘系数；rrf/weighted 两模式都乘；准入集合不变

- [ ] **Step 1: 写失败测试**（新建 `internal/index/query_recency_test.go`；Chtimes 先例照抄 sync_fastpath_test.go:72-79）

```go
package index

import (
	"os"
	"path/filepath"
	"testing"
	"time"

	"openknowledge/internal/config"
	"openknowledge/internal/retrieve"
)

// TestQueryRecencyDemotesStale 陈旧条目乘系数后在近似同分时让位：
// 两条 note 都命中"苹果"，单关键词通道 RRF 名次分 1/61 与 1/62（谁前谁后由
// BM25 决定，两种排位的分差都 < 0.85 翻转所需）——stale 条目（400 天，
// ≥ note stale 180）×0.85 后必排 fresh 条目之后；关闭 recency 则不上榜。
func TestQueryRecencyDemotesStale(t *testing.T) {
	dir := t.TempDir()
	kdir := filepath.Join(dir, "knowledge")
	writeEntryFile(t, kdir, "old.md", "---\ntitle: 苹果旧笔记\ntype: note\ntags: []\n---\n\n苹果 香蕉。\n")
	writeEntryFile(t, kdir, "new.md", "---\ntitle: 苹果新笔记\ntype: note\ntags: []\n---\n\n苹果 橘子。\n")
	old := time.Now().Add(-400 * 24 * time.Hour)
	if err := os.Chtimes(filepath.Join(kdir, "old.md"), old, old); err != nil {
		t.Fatal(err)
	}
	db, err := Open(filepath.Join(dir, "kb.db"))
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { db.Close() })
	if err := db.Sync(kdir, nil); err != nil {
		t.Fatal(err)
	}
	rc := config.RetrieveRecency{Enabled: true, Floor: 0.85, Windows: config.RecencyWindows{Note: []int{60, 180}}}
	// 开启：new.md 在前，old.md 分数被乘 0.85
	on := config.Retrieve{Alpha: 1, Beta: 1, TopN: 5, Recency: rc}
	hits, info, err := db.QueryEx(retrieve.Terms("苹果"), nil, on)
	if err != nil {
		t.Fatal(err)
	}
	if len(hits) != 2 {
		t.Fatalf("应命中 2 条: %+v", hits)
	}
	if hits[0].Filename != "new.md" || hits[1].Filename != "old.md" {
		t.Fatalf("陈旧条目应让位: %+v", hits)
	}
	if hits[1].Score >= hits[0].Score {
		t.Fatalf("old.md 应被乘 0.85: %+v", hits)
	}
	if hits[0].Mtime <= 0 || hits[1].Mtime <= 0 {
		t.Fatalf("Hit 应携带 Mtime: %+v", hits)
	}
	// 关闭：分数不乘系数、不上榜
	off := config.Retrieve{Alpha: 1, Beta: 1, TopN: 5}
	_, infoOff, err := db.QueryEx(retrieve.Terms("苹果"), nil, off)
	if err != nil {
		t.Fatal(err)
	}
	if len(infoOff.RecencyShifted) != 0 {
		t.Fatalf("关闭时不应有时效观测: %+v", infoOff.RecencyShifted)
	}
	_ = info
}
```

注意：RecencyShifted 的上榜判定依赖"名次是否变差"，而两条目的 BM25 名次在本 fixture 下不可预知（2 文档语料 idf 趋零）——所以集成测试只断言**分数序**（`hits[0]==new.md` 在两种 BM25 排位下都成立：A 前则 0.85/61<1/62 翻转，B 前则 0.85/62<1/61 保持）；RecencyShifted 的翻名次判定由 Task 2 的 `TestApplyRecency` 单测确定性覆盖，此处只断言"关闭时为空"。若实现者验证发现某 BM25 排位下断言不成立，停下来报告 DONE_WITH_CONCERNS，不要擅自改断言。

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/index/ -run TestQueryRecencyDemotesStale -v`
Expected: FAIL（`RecencyShifted` 未接线——Mtime 为 0 / 无系数 / 无字段编译错，视 Task 2 合入状态）

- [ ] **Step 3: 实现**（`internal/index/query.go` 四处）

**① QueryInfo（:65-74）加字段 + 更新 doc 注释：**

```go
// QueryInfo 描述一次 Query 的诊断信息（供 hook 日志 / ok search 提示 / GUI 日志页
// 展示）。语义通道字段（SemanticRejected 等）仅当语义通道参与（向量存在）且
// 样本 ≥3 时有意义；RecencyShifted 仅当时效衰减开启且有条目因系数名次变差时非空。
type QueryInfo struct {
	// SemanticRejected：语义通道参与但全部候选被门槛拒绝（无显著头部）。
	SemanticRejected bool
	Coses            int
	MaxCos           float64
	MedianCos        float64
	RelGap           float64
	// RecencyShifted：因时效系数（retrieve.recency）名次变差的条目
	//（"filename×0.85" 格式，按新名次排序）。
	RecencyShifted []string
}
```

**② 关键词 SELECT 加 mtime**（:167-172，列加在 `bm25(...) AS r` 之前）：

```go
		rows, err := db.sql.Query(
			`SELECT e.filename, e.title, e.type, e.summary, e.body, e.tags, e.mtime,
				bm25(entries_fts, 10.0, 8.0, 3.0, 1.0) AS r
			FROM entries_fts JOIN entries e ON e.filename = entries_fts.filename
			WHERE entries_fts MATCH ? AND e.mandatory = 0 AND e.draft = 0`, match)
```

Scan（:176-194）相应改为：

```go
		for rows.Next() {
			var h Hit
			var tagsStr string
			var rank float64
			if err := rows.Scan(&h.Filename, &h.Title, &h.Type, &h.Summary, &h.Body, &tagsStr, &h.Mtime, &rank); err != nil {
```

**③ 语义 SELECT 加 mtime**（:206-209）：

```go
		rows, err := db.sql.Query(
			`SELECT e.filename, e.title, e.type, e.summary, e.body, e.tags, e.mtime, v.blob
			FROM vectors v JOIN entries e ON e.filename = v.filename
			WHERE e.mandatory = 0 AND e.draft = 0`)
```

Scan 段（:213-234）：加 `var mtime int64`、Scan 参数在 `&tagsStr` 后加 `&mtime`（blob 保持最后）、`cands = append(cands, cand{Hit{...}, cos})` 字面量加 `Mtime: mtime`。

**④ 尾部乘系数**（:276-293，`applyRRF` 调用之后、收集 `out` 之前）：

```go
	if fusion == "rrf" {
		applyRRF(hits, kwNorms, cosScores, cfg.RrfK)
	}
	// 时效信号：融合分之后乘系数、不参与准入——陈旧条目在近似同分时让位；
	// rrf/weighted 两模式都乘。返回的名次变差清单进 QueryInfo 观测。
	info.RecencyShifted = applyRecency(hits, time.Now().Unix(), cfg.Recency)
```

（`time` 若未 import 则补。）

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/index/ -v`
Expected: PASS（含既有全部绿——SELECT 加列对既有断言透明）

- [ ] **Step 5: Commit**

```bash
git add internal/index/query.go internal/index/query_recency_test.go
git commit -m "feat(index): queryAll 接入时效系数（Hit.Mtime 取数 + RecencyShifted 观测）"
```

---

### Task 4: 观测链路（hook ok.log + CLI stderr）+ 文档

**Files:**
- Modify: `internal/hook/core.go`（SemanticRejected 日志块 :146-157 之后）
- Modify: `internal/cli/cli.go`（SemanticRejected stderr 块 :344-352 之后）
- Modify: `docs/ARCHITECTURE.md`（17.5 融合段 :862-866 区域 + 配置表 :976-981 区域）
- Test: `internal/hook/core_test.go`

**Interfaces:**
- Consumes: `QueryInfo.RecencyShifted`（Task 3）
- Produces: 无新符号；行为契约 = 有时效降权时 hook 记 `prompt recency:` 一行、ok search 打 stderr 一行

- [ ] **Step 1: 写失败测试**（追加到 `internal/hook/core_test.go`）

构造真实翻名次在 hook 层不可控（BM25 排位不可预知，同 Task 3 注释），故 hook 侧只测**接线**：条目陈旧 + recency 开启时，若 QueryInfo 带 RecencyShifted 则 ok.log 有 `prompt recency` 行——但翻名次不保证发生，无法确定性断言日志出现。**裁决：hook 不加测试**（观测链路是 3 行透传，由 Task 3 集成测试 + 终审人工核对覆盖）；本 Step 改为**验证性阅读**：实现后 Grep 确认 hook/cli 两处接入点。TDD 对本任务退化为"先写接入点、编译+既有测试全绿"。

- [ ] **Step 2: 实现**

`internal/hook/core.go` SemanticRejected 日志块之后加：

```go
		// 时效衰减改变了排序：记 ok.log，GUI 日志页可按"时效"过滤
		if len(info.RecencyShifted) > 0 {
			logErr("prompt recency: 时效衰减改变排序（%s）", strings.Join(info.RecencyShifted, "、"))
		}
```

`internal/cli/cli.go` SemanticRejected stderr 块之后加：

```go
	if len(info.RecencyShifted) > 0 {
		fmt.Fprintf(stderr, "时效衰减改变排序（%s）\n", strings.Join(info.RecencyShifted, "、"))
	}
```

（两处若缺 `strings` import 则补。）

`docs/ARCHITECTURE.md`：

1. 17.5 节融合代码块（RRF 轮已改，`grep -n "rrf_k" docs/ARCHITECTURE.md` 定位）下方加一行：

   > 融合分之后、排序之前乘时效系数（`retrieve.recency`，默认启用）：mtime age ≤ fresh 天 → 1.0，≥ stale 天 → floor 0.85，中间线性；窗口按条目类型分（[retrieve.recency.windows]）。不参与准入。

2. 配置表（`grep -n "retrieve.rrf_k" docs/ARCHITECTURE.md` 定位）rrf_k 行后加两行：

   ```
   | retrieve.recency.enabled / floor | true / 0.85 | 时效信号：按 mtime 新鲜度给融合分乘系数（不参与准入）；floor 为陈旧系数下限 |
   | retrieve.recency.windows.* | rule/reference [180,730]、pitfall [90,365]、note [60,180] | 各类型 [fresh_days, stale_days] 窗口；全零或非法 = 该类型不衰减 |
   ```

- [ ] **Step 3: 验证**

Run: `go build ./... && go test ./internal/hook/ ./internal/cli/ -v && grep -n "prompt recency" internal/hook/core.go && grep -n "时效衰减" internal/cli/cli.go docs/ARCHITECTURE.md`
Expected: 全绿；三处接入点 grep 命中

- [ ] **Step 4: Commit**

```bash
git add internal/hook/core.go internal/cli/cli.go docs/ARCHITECTURE.md
git commit -m "feat(hook,cli,docs): 时效观测链路（ok.log/stderr）+ ARCHITECTURE 配置表"
```

---

### Task 5: changelog + 全仓回归

**Files:**
- Create: `docs/changelogs/2026-08-16-recency.md`

**Interfaces:**
- Consumes: Task 1-4 全部
- Produces: 发布素材

- [ ] **Step 1: 写 changelog**（格式对照 docs/changelogs/2026-08-16-rrf-fusion.md）

```markdown
# 2026-08-16 时效信号：按 mtime 新鲜度给融合分乘系数（retrieve.recency）

- **问题**：零时效信号——两年前的过时 pitfall 与上周的 rule 同分竞争 top_n=2 的注入名额。
- **修复**：
  - `recency.Factor`：age ≤ fresh_days → 1.0，age ≥ stale_days → floor（默认
    0.85），中间线性；按条目类型分窗（[retrieve.recency.windows]：
    pitfall [90,365] / note [60,180] / rule、reference [180,730]）；
  - 作用位置：融合分（RRF/weighted 均在）之后乘系数、**不参与准入**——陈旧
    不等于无关，陈旧条目仍可准入注入，只在近似同分时让位；RRF 名次尺度下
    0.85 足以翻转相邻名次而不可能翻越大分差（温和决胜）；
  - 信号源 `entries.mtime`（文件系统 mtime，Unix 秒，sync 已维护，无 schema
    变更）；**编辑条目即刷新新鲜度**视为 feature——被持续维护的条目保持新鲜；
  - fail-open：类型未知、窗口全零/非法、mtime<=0、floor 非法一律不衰减；
  - 观测：系数使条目名次变差时 hook 记 ok.log `prompt recency:` 一行、
    ok search 打 stderr（GUI 日志可按"时效"过滤）；
  - 配置 `[retrieve.recency]` enabled（默认 true）/ floor（默认 0.85）/
    windows 子表；GUI 暴露列为跟进项。
- **注意**：mtime 是文件系统 mtime——批量重格式化/迁移条目文件会整体刷新
  新鲜度（可接受，如需"取代"语义另议 superseded_by）。
- **测试**：`TestFactorBoundaries`（四类型 fresh/stale 两端 + 线性中点 +
  未知类型）/`TestFactorFailOpen`（mtime=0、floor 非法、窗口全零/长度错/
  stale<=fresh）/`TestApplyRecency`（翻名次上榜/不翻不上榜/关闭不动）/
  `TestQueryRecencyDemotesStale`（真实库集成：陈旧让位 + Mtime 取数 +
  关闭对照）/`TestRecencyConfigDefaultAndOverride`（配置四态）；
  全仓 go test ./... 绿。
```

- [ ] **Step 2: 全仓回归**

Run: `go test ./...`
Expected: 全部 PASS

- [ ] **Step 3: Commit**

```bash
git add docs/changelogs/2026-08-16-recency.md
git commit -m "docs: 时效信号 changelog"
```
